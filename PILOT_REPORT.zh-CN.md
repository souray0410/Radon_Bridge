# R&B 首轮轻量验证报告

> **状态更正（2026-09-04）：** 其中共享头实验不符合用户要求的“独立预测器 + 中间特征桥接”；原结果仅保留作历史探索，不构成该方案的有效性验证。单模态/晚期平均也未按 macro-F1 选优。详见说明书审计。


日期：2026-09-04。阶段：数学/工程正确性与真实数据反馈；不是论文最终实验。

## 当前结论

**代码与跨维度通信链路已经成立，但两轮小实验尚未观察到 R&B 的验证性能优势。**
目前不支持宣称 Radon 优于普通投影，也不支持据此否定其在更好数据、主干和任务上的价值。

本轮只用了 GPU 1；两轮主实验分别耗时 1.82 和 1.98 分钟，总计约 3.80 GPU 分钟
（0.063 GPU 小时，按整个训练进程占用时间计）。主实验最大进程显存 2562 MiB，
小容量轮为 1790 MiB。首次影像缓存准备另需数分钟，代码实现和审计时间不包含在训练时间里。
24 GPU 小时未作为需要花满的预算。所有 R&B 进程已结束，GPU 1 已释放；未中断 GPU 0 上的其他任务。

## 实现与正确性

- 独立私有 RadonBridge 项目，MHD V4 固定为 `8f6651ce9af96f54585bf32bbfd70afe0315867d`。
- Project、Handoff、Mixer、Expand、Return、Residual 及主干阶段显式纳入 MHD。
- Householder 定义方向变换；固定坐标下多线性插值与超平面积分构造离散投影矩阵。
- Return 使用离散矩阵的精确转置；不把伴随算子叫作逆 Radon 变换。
- 1D–4D 方向映射、伴随内积、双向梯度检查通过。
- 二维/三维平移高斯体的解析积分相对峰值误差分别为 2.33% / 1.82%。
- 小网络 MHD 与原生 PyTorch 输出、梯度及 SGD 更新一致；梯度最大差异为 0。
- 零 Mixer 时精确恢复无通信网络；真实预训练主干 16 人、40 步拟合检查达到 100% 训练准确率。

当前采用小特征图的稠密参考算子，易于验证和控制显存；尚未实现大尺度矩阵自由后端，
不能把通过 1D–4D 小张量测试等同于已经证明任意维的计算可扩展性。

## 数据与模型

- 256 名训练参与者、128 名验证参与者，两类在各集合内平衡，参与者交集为 0。
- 保留已有患者划分。使用参与者级病历定义青光眼标签，双眼聚合后分类，没有测试集评估。
- CFP 为 96×96；OCT 每眼按完整扫描范围均匀抽取 32 张有序 B-scan，每张 96×96。
  这是低分辨率三维输入，并非中央单张 OCT，也不是使用全部原始切片的全分辨率实验。
- 768 只眼中，764 个体的原始切片为 512×650，4 个为 512×480；已处理并记录该异质性。
- CFP 使用官方 ImageNet ResNet18；OCT 由对应二维滤波器沿深度扩展、平均初始化三维卷积。
  这不是官方 OCT 预训练权重。阶段 3 的特征分别为 `[256,6,6]` 与 `[256,8,6,6]`。
- 在同一轮中，四组由同一 warmup checkpoint 分叉，样本顺序、优化器配置、训练轮数保持一致。
  固定 seed 不意味着 CUDA 算法逐位确定；本轮没有作逐位可复现的声明。

## 两轮反馈

| 配置 | 继续训练基线 | R&B | 空间打乱投影 | 仅分支内部滤波 |
|---|---:|---:|---:|---:|
| Handoff 25%，末阶段可训练 | 0.63147 | 0.62817 | 0.62549 | 0.62769 |
| Handoff 3.125%，通信阶段固定主干 | 0.64624 | 0.64648 | 0.64624 | 0.64600 |

表中为验证 AUROC，各自按验证 AUROC 选择 checkpoint。

第一轮 R&B 相对基线差值为 −0.00330，参与者配对 bootstrap 区间为 [−0.04311, 0.03359]。
第二轮差值为 +0.00024，区间为 [−0.00198, 0.00244]。区间跨零，观察差异不能作为正结果。
同一验证集也用于 checkpoint 选择，因此这些区间只是探索性描述，不是独立确认性推断。

第一轮末阶段与大 Bridge 在小队列上明显过拟合：所选继续训练基线的训练 AUROC 0.9996、
验证 AUROC 0.6315。通信模块约 1756 万参数，对 256 人而言过大。
第二轮将通信参数降至约 142 万，并在 warmup 后冻结主干，只训练分类头和 Bridge；
所选基线训练/验证 AUROC 为 0.8121 / 0.6462，验证 log loss 从约 2.14 降至 0.73。
容量和训练策略同时改变，所以不能单独把变化归因于 Handoff，也不构成正式消融。

### Bridge 是否实际上没有工作？

不是。第二轮最佳 checkpoint 的 CFP/OCT 残差范数中位数约为原特征的 0.80% / 0.24%；
关闭跨分支权重后，预测概率平均绝对变化约 0.0093。
到最后一轮，该比例达到 18.1% / 15.2%，关闭跨分支通信的概率变化约 0.0322，
但验证收益没有随之出现。可排除“完全无通信”的简单实现问题；尚不能保证通信学习得足够好。

### 模态信息是否足够稳定？

在第二轮 warmup 的固定特征上，以预先固定 C=0.01 的标准化 L2 Logistic Regression 做探针：

| 特征 | 训练 AUROC | 验证 AUROC |
|---|---:|---:|
| CFP | 0.8947 | 0.5405 |
| 三维 OCT | 0.8143 | 0.6133 |
| 拼接 | 0.9564 | 0.6011 |

这提示当前设置中 CFP 的泛化信号很弱，简单增加模态也没有稳定收益。
这些是联合训练主干的特征探针，不是独立训练的单模态基线；不能据此断言 CFP 本身无价值。

## 下一阶段的优先顺序

1. 先改善基线的输入与标签信号：96×96 CFP、6×6 中间网格可能不足以保留青光眼细节；
   优先检查更高分辨率和图像质量，建立合格的独立单模态基线，再讨论通信增益。
2. 在同一批参与者上审计同期 logMAR、年龄及结构表型的完整性，选择更可靠的技术验证任务；
   不以哪个标签让 R&B 涨分最多作为选题依据，也不把 OCT 导出的厚度预测当作独立临床发现。
3. 基线信号与互补性明确后，再进行相同初始化的 R&B / 内部滤波 / 空间打乱对照及少量种子重复。
4. 获得稳定增益后，才投入 generic learned projection、cross-attention、不同阶段和外部数据。

这一阶段不扩大拓扑、不开展全数据大扫参，不根据当前结果宣称达到任何期刊的录用水平。

## 溯源

- 初始主实验源码：`f1024924e1bd8c8fdcc7d08cd4e1a84482c9edcf`。
- 小容量主实验源码：`b04d93e`；诊断脚本：`radonbridge/diagnose.py`。
- ws 源码：`/home/mengh/RadonBridge`。
- ws 运行数据：`/data/mengh/RadonBridge`；主实验子目录为 `runs/pilot3407`、`runs/small3407`。
- 各运行保存配置、源码提交、输入清单哈希、初始化权重哈希、逐轮历史、模型与优化器状态及验证预测。
- 原始影像、个体标签和预测只保留在 ws；本报告只含聚合结果。
