# CI example

checks.example.yml is an inactive GitHub Actions configuration. The current upload credential lacks workflow scope. Local and Ibex checks are run directly; no hosted CI pass is claimed. A maintainer can activate the reviewed file under workflows/ when that permission is available.
