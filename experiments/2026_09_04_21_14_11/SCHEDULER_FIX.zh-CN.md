# 调度修正

上一时间戳第一阶段已按平台期完成。控制器用预检中一次受共享GPU干扰的慢step外推整组成本，错误地在仍有约24.04 GPU分钟时拒绝启动第一对照。训练和模型本身没有失败。

本时间戳只修正调度：显式继承并校验上一轮第一阶段summary与两个检查点SHA；后续成本使用实际完整epoch耗时乘1.2，并以已完成阶段轮数乘1.5估计轮数。每完成一组，用该组真实轮数更新下一组估计。硬GPU预算、平台期规则、模型和算法均不变。
