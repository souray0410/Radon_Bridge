# 当前验收

CPU二维至四维投影/直接反投影相对误差最大6.76e-16；二维EEM角间隔误差最大4.06e-7度。异构2–4参与者、跨深度、多个不同M/S/rho桥、原Node ID/下游连接、MHD与原生梯度、零通信预测和跨网络梯度通过。

比例边界和异构宽度不扩张，六组加载相同原生权重后初始预测完全一致，优化器全部重新建立且无冻结参数。真实双桥microbatch16通过，进程峰值8556MiB（8.36GiB）；全部模块更新，双向跨网络梯度非零。当前实际预检轮次耗费0.6023GPU分钟，包含重复调度验收，已入账。

平台期与epoch上限区分、微批累积梯度、双GPU调度/计费/配对预算保护通过。训练时还会验证实际检查点SHA、模态/种子/平台期来源、加载后的预测匹配；未通过不得继续。

验收源码SHA与协议SHA见preflight.json；真实实验效果尚未产生。历史固定H几何报告只作不变算子的溯源，不冒充新版训练结果。
