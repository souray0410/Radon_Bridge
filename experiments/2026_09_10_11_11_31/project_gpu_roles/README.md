# 项目与基础模型GPU管理验收

已部署独立角色预算与项目优先管理入口。初始LOOK/Radon_Bridge各预留2张，基础模型上限20张；额度包括运行及待批，实际学校限制仍实时核验。使用共享账号锁、原子模型领取和完整断点恢复。

部署时4个原项目allocation持续运行。旧GPU owner保留原科学源码，正常轮换后进入新版；没有为上线停止健康训练。CPU调度新入口已接替，原有活跃salloc子进程的旧CPU会话由guardian保留到子进程结束。实时项目让位仍须实际共存资源预检，不以部署成功替代运行验收。

配套代码测试在ws02通过LOOK/共享管理51项、Radon_Bridge12项。原LOOK补充实现另有完整测试记录。LOOK新增378次神经网络训练及机制/样本曲线共7182逻辑位置，当前等待原81宿主验收；不是7182次新模型训练或已完成结果。test继续封闭。

基础模型候选、配置和选中完整权重独立保存。暂停模型不改run ID，不删除恢复状态；历史last.pt仍为验收/下游依赖，不为新存储规则立即删除。详见各项目协议、Model_Training的模型运行标准及RETENTION.md。
