# R&B：72项必要补充与统一test核验

用户2026-09-07批准本协议。现有384项、最终历史213项整合清单、18项S轴实验、原父模型以及新增72项均进入注册表。联合宽度9次训练展开27个视图；重复源清单不累加，同检查点/推理配置/状态去重并保留引用。末端可学习融合协议继续撤销。

## 数据边界

原reference_labels.csv SHA256为eae604091a1094ed70ff4edbcf6e59d00b57b5be124fb4ac5f76ece1beac0d8d；train1264、development296、test290，参与者互斥。当前只允许train/development训练和验收。原始盘未接入；工作站test每眼只有一张导出OCT，必须取得完整体积按既有32层均匀抽样规则构建缓存，不能复制单切片替代。

test预测前必须完成历史使用审计、完整数据和模型锁定以及开发集预测重放。审计发现污染或无法解释的相关使用记录时停止解封。仅有test_used=false声明不能证明全部外部使用历史。所有新预测/诊断使用独立评价记录，不改历史训练test_used字段。

## 唯一新增训练矩阵

1. 非中心化SVD、S64/k3，新增(M,r)=(16,16),(64,32)，每组Radon/同宽普通通信×seed3416–3418×backbone LR3e-5/6e-5，共24项，补齐M16/32/64×r16/32网格。h=rM，rho=r/256。固定r并不固定参数和算术量。
2. 冻结原生网络、分类头和BN；SVD/QR×Radon/普通通信/自身处理×rho1/16,1/8,1/4×三种子，共54项；复用现有6项，新增48项。stage3/M32/S64/k3；桥LR1e-4，原生LR=null。只训练无偏置零初始化卷积，自身掩码为块对角，不称有效容量完全匹配。固定基直接复用历史文件。

全参数组原生BN正常更新。batch16、AdamW WD.01、各分支/模块裁剪5、至少8轮、连续6轮无超过.001改善判平台、3轮停滞LR×.3、60轮仅保护上限。选优均为两分支macro-F1平均。冻结组逐轮和选中模型验证原生参数/BN SHA不变。完成72项后不继续搜索。

## 预定比较与统一诊断

新增17项主要比较由pretest_study.contrast_definitions固定：allocation8（固定r的M32/M64减M16几何增益4项；跨M的r32减r16在Radon/普通及交互3项；M×r几何交互1项）；frozen9（两基Radon减普通/自身/父模型6项；基×几何1项；两基全参数减冻结几何增益2项）。全参数历史统一使用6e-5；跨种子、相应rho/M等权。

统一test保留原明确报告的比较，加入17项，按规范化权重去重。10,000次参与者bootstrap，全部模型共享索引；普通95%、原比较族max-|t|区间和全部主要比较全局max-|t|区间同时报告，整体主张采用全局区间。零方差及不可估计单列。test/development不做跨集合参与者配对，差异采用独立集合重采样。±1pp是研究范围而非临床阈值。

24个增强检查点四状态共96视图；历史SVD/QR×三方向×三rho×三seed共54检查点，正确配对和三关闭状态，加三种打乱各20次，共64状态/检查点。20组无自配对置换与标签无关，双眼整体移动；双向用置换及逆。已有paired代码逐批核验缓存前向等于完整MHD；推理干预不称因果协同。梯度/基拟合只在train。

主指标两分支macro-F1平均，同时列CFP/OCT；辅助AUROC、precision/recall、混淆、NLL、Brier、固定概率平均。所有检查点及阈值在test前固定，不用test选任何配置。支持/反例/未确定分别记录；不显著不等于无效。

## 数据复制、执行与交付

原始盘接入后按文件清单、大小、SHA、链接和数量验收复制，原压缩包不全量解压。backup与backup_extra是同一文件系统。至少100GiB+活跃峰值余量；空间不足只复制本轮必要数据并标子集，不能冒称完整UKB；原盘不删除，历史证据不清理。

隔离检出测试，提交GitHub后从不可变工作树执行；双GPU独立滚动派发，每卡项目≤10GiB，不影响LOOK。22种新/复用结构形状在train做batch16预检。OOM、失败、未平台停止待处理；中断只恢复未接受项，完整checkpoint/优化器/失败attempt保留且校验后归档。

新增训练可在等原始盘期间运行；统一test需全部前置门槛通过。每三小时监控，健康静默，失败/需处理/完成通知。交付全部源清单、开发/test表、比较族和全局区间、图、成本、环境、协议偏离及主张—证据—反例表。参与者级数据不进GitHub。最终交付后停止监控，扩数据/模型另立协议，当前test不得用于后续调参。
