# 原始影像子集接入：2026-09-07

用户接入My Book并只读挂载后，清点得到左/右眼OCT目录共175212个ZIP，合计4430.088 GiB。现有backup不够完整拷贝，用户明确改为仅复制当前研究所需数据，优先完成复制以归还移动硬盘。

以既有reference_labels.csv SHA256 eae604091a1094ed70ff4edbcf6e59d00b57b5be124fb4ac5f76ece1beac0d8d锁定1850人（train1264、development296、test290）。逐行核对训练/开发缓存清单，参与者无重复且test互斥；不得重新筛选或根据缺失改样本。清点7400个文件全部存在：3700份原生分辨率导出CFP、3700份完整OCT ZIP，总108441200890字节，约100.994 GiB。

CFP来自原训练使用的工作站导出影像目录；OCT来自已挂载移动硬盘。按既有相对路径保留目录结构，目标 `/backup/mengh/UKB/RadonBridge_2026_09_06_14_05_08/data`。该目录是当前队列所需子集，不是完整UKB或完整CFP/OCT数据库。

执行工具 tools/ukb_subset_transfer.py：独占复制锁、源清单与缓存SHA验证、复制前容量门槛、逐文件源SHA与目标SHA核对、可恢复且核对前缀的partial文件、原子完成文件、进度和完整接受清单。预留100GiB安全空间、64GiB活跃实验输出和32GiB预处理空间。源文件不修改，不覆盖不匹配的既有副本，不上传原始数据、参与者路径或预测到GitHub。

复制test原始字节并做完整性校验不是模型评价。复制完成不代表test解封：仍需一致预处理/32层体积缓存、LOOK与R&B历史test使用审计、所有训练完成、模型注册与比较锁定、开发集重放。训练入口继续禁用test，旧摘要test_used=False不改写。此操作没有改变队列、标签、数据划分或主要分析。

只有transfer_status.json为verified且逐文件清单完整匹配后，才告知移动盘可卸载。后续数据处理改读已验证的backup副本，不能因原盘拔出再回退单切片OCT。
