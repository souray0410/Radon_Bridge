# R&B：功能依赖与冻结原网络补充协议

2026-09-07用户授权在原378项分支协议之后补充。原378项完整保留，不改已运行源码、不加末端融合头。本补充在原队列完成并释放项目锁后独立执行，总训练结果位置增至384项；其中6项冻结实验单列，不能与378项全参数结果混排。诊断不计训练次数。

## A：组件开关（不训练）

24个已选宿主增强检查点：MMTM隐层256/注意力d256 × 新增Radon/普通通信 × 三种子 × 两学习率。每个检查点执行四种状态：两路开、仅宿主、仅新增、两路关。只置零对应通信残差，不删除原生特征路径，原Node ID保持不变，不更新任何参数/BN/优化器/RNG。不缓存重排参与者，用完整batch16 MHD前向。双眼共同属于原参与者。

两路开必须匹配保存的选中预测；两路关必须匹配独立构建并载入同一微调原生参数的无桥图。它不是原独立预训练网络，后者预测另列。关闭宿主和新增通路后的任务概率/F1变化反映检查点的功能依赖；推理分布变化和F1非线性意味着不能据此宣称严格因果或临床协同。

开发集296人完整评价共96视图。两宿主×两新通路×两个删除效应，8项分支平均F1比较独立成族，10,000次共享参与者bootstrap，普通及max-|t|区间；逐分支、逐种子、逐学习率、四状态全部保留。F1四格非加性仅描述，不包装为生物学协同。

## B：冻结原网络（6项训练）

种子3416/3417/3418，各自原独立最佳CFP/OCT父模型及原训练集SVD基。固定stage3/M32/S64/k3/r16/h512/rho1/16，标准Radon与同基同宽行范数匹配普通重采样各三项。两条主干、原分类头及所有原生BN缓冲区固定，原生模块eval；仅kernel3无偏置零初始化卷积可学习。禁止no_grad包住整个后半段，梯度必须经冻结网络正常传回桥。backbone/head LR不适用，显式null；bridge LR1e-4，AdamW WD.01，通信梯度裁剪5。

batch16、原数据顺序、CE_CFP+CE_OCT、按分支平均F1选共同检查点及平台；min8/patience6/min_delta.001/LR3轮×.3/max60保护不变。每轮以及选中检查点核验原生参数/BN SHA不变。仅桥进入优化器；验证桥更新、固定基不变、开通后跨来源输入梯度存在、零初始化匹配原父预测。无桥参照直接读取三种子的原父预测，不增训练。

主要比较冻结Radon减冻结普通通信、冻结Radon减原父预测，两项独立成族，10,000次共享参与者bootstrap及max-|t|区间。原全参数同配置的两学习率结果分别作描述参照，不给冻结实验虚构backbone学习率。冻结失败不推翻全参数方法，只限定是否依赖共同适应。新补充均是开发集探索，不读test。

## 执行与交付

独立目录dependency_supplement，代码版本与协议锁定。旧branch_only入口保持其原不可变提交；新入口scripts/run_dependency_supplement.py等待旧378项训练、诊断和报告完成且项目锁释放，再启动。等待者不占GPU，不恢复已撤销的融合队列。

两个冻结模式各做batch16三次完整优化器更新预检，四类宿主/新增组合各做四开关batch16训练数据只读预检。全部通过后才训练/评价。双GPU独立派发，每卡项目≤10GiB，工作和备份盘100GiB余量及活跃峰值，GPU不限时但记账，不动LOOK。OOM、失败、未平台不得作为完成，不自动改变限制。完整接受、SHA归档后退休工作副本，保留全部检查点/优化器/失败attempt。

单独报告6训练和96评价视图、2项冻结/8项组件依赖比较、逐分支和全部原始结果、成本与范围限制。图表命名R&B（Radon Bridge）。聚合图表/协议/代码可同步GitHub，参与者预测及特征留远端。每三小时自动检查，最终主研究及本补充全部交付后才暂停监控。
