# R&B：撤销末端融合协议，继续分支机制研究

用户在2026-09-07明确要求停止并从当前研究删除带可学习末端融合头的任务，回到两个已有分类网络及中间通信。此决定发生在分支342项和部分融合结果已经揭示后；它是研究范围变更，不是融合方法无效的证明。旧协议、失败、负结果、配置、检查点和预测作为撤出本轮的历史证据保留，不覆盖、不混入新主报告。

当前范围为378项：342项已验收分支直接结果，36项原定分支宿主增强。固定MMTM隐层256和注意力d256宿主，各两学习率、三种子，比较继续训练、增加R&B、增加同基同宽普通通信。宿主配置不按表现更换，即使选中epoch0也如实保留。没有新的末端融合头；损失仍为CE_CFP＋CE_OCT，按两分支macro-F1平均选优、判平台及调度学习率。固定概率平均仅作为不增加参数的次要读出。

M/S/k/r/h、SVD、父检查点、数据、种子、学习率、batch16、AdamW、梯度裁剪、BN、平台规则及每卡10GiB上限均沿用EXECUTION.zh-CN.md。所有已完成分支配置与检查点SHA逐项接受，不重复训练；两卡独立滚动派发，只读诊断、宿主基拟合和新增模式资源预检继续。GPU时间无限但记账，不影响LOOK，不读test。

统计保留原协议分支部分的28项直接及4项增强对比定义，10,000次共享参与者bootstrap。比较族从56/8缩小到28/4发生在看过部分结果后，必须明确披露；新区间不能冒称原56/8项同时校正，也不作为独立确认性证据。被撤销的融合结果不列入分支排名，撤销原因和已观察到的反例仍可追溯。

旧队列设置drain.request，不再派发融合任务；最后在途任务自然结束后退出。旧根目录scope_withdrawal.json指向同时间戳下branch_only/新执行目录。新目录只复制协议/索引/验收清单，完整模型引用原已验收归档，不复制数百GiB模型。source_snapshot保留迁移时原清单及SHA。原candidate_lock和原manifest不改写。最终报告输出branch_only/report；工作副本在完整归档校验后退休。

只允许新入口scripts/run_branch_mechanism.py；旧两协议入口不得恢复。监控三小时一次，正常无通知；仅故障、需要处理及全部最终交付时通知。
