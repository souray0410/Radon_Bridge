# R&B：2026_09_06_14_05_08 研究问题与汇报衔接

## 最新目标修正（用户 2026-09-06 明确要求）

后续将更换数据与网络。本轮的目标是验证关键机制、发现适用条件和反例，而不是找出当前小队列的最佳 M/S/k。原全排列矩阵及开发集最优选型属于此前方案；最终训练子集尚待明确，不能因此启动旧版大规模搜索或自动按开发集最优选择增强通路。

先继续不依赖矩阵取舍的工程工作：完整归档、恢复验收、默认路径兼容、可变核长与精确 r/h 接口、同输入并行残差、计数器。既有结果保留，不重新解释为独立验证。

## 以导师讨论中的问题组织证据

| 讨论问题 | 当前可验证的内容 | 必要对照／材料 | 不能据此声称 |
|---|---|---|---|
| 为什么要用 Radon 空间？ | 预先固定资源条件下，Radon 与普通通信的差值 | 同基、同宽、同核长；另列计算匹配，保留已有空间打乱及 S 轴打乱 | 投影轴相同就完成解剖配准或语义对齐 |
| 只是新增参数或继续训练带来的收益吗？ | 结构收益相对继续训练和普通通信的增量 | 相同父模型、优化器重置、训练规则；参数和计算分别核算 | 所有配置或方法均完全等容量 |
| SVD 是核心创新还是实现手段？ | 几何收益在不同压缩实现下是否存在 | 复用已有 SVD、QR、可学习通道证据；不再搜索压缩新方法 | SVD 能量保留保证保留全部疾病信息 |
| M 增大究竟改变了什么？ | 固定 h 后，方向与通道的资源分配效应 | r=h/M 明示；必要时结合既有固定 r 结果 | 固定 h 的 M 对照纯粹隔离了方向几何 |
| S 与核长有什么作用？ | S 轴采样及局部混合的敏感性 | 代表配置下改变 S/k，结合既有邻接打乱；比较归一化跨度 | 固定 k 改 S 保持相同物理邻域；跨 k 自动等参数 |
| 两个通信方向是否都有用？ | 每个分支的方向特异变化和受损情况 | 复用单向、双向、关闭通信与错误配对诊断 | 双向连接必然让两路都改善；扰动等于临床因果证明 |
| 为什么不用已有非线性融合？ | 直接性能／成本比较，以及 R&B 对完整宿主的附加价值 | 宿主继续训练、宿主加 R&B、宿主加普通通信；同输入并行残差 | 非线性必然不稳定；线性天然更可解释 |
| 是否即插即用？ | 兼容已有特征接口，零初始化恢复完整宿主，训练后效果 | 严格宿主加载、原 Node ID、前后向、零初始化；注明全部微调 | 插入后无需训练即可获得增益；适配任何模型均有效 |
| 到底汇报哪个性能？ | 分支协议和融合协议独立评价 | 分别写清损失、选优、平台、主要指标；固定概率平均仅作辅助 | 两分支 F1 平均等于融合预测 F1；有独立头就只依赖本模态 |
| UKB 为什么只有这一小部分？ | 当前固定样本、筛选历史与可追溯限制 | 抽样流程、平衡策略、排除规则；原始数据恢复后另做审计 | 严格筛选自动得到更可靠临床证据；老师提到的几万人必然满足本任务全部条件 |
| 标签是不是足够可靠？ | 核对实际字段、来源与时间关系 | 当前来源材料；以后审计自报、临床记录、ICD 等实际构造 | 未核实就称专家金标准，或笼统称全部标签只是自报 |
| 能否迁移到别的任务、器官或模型？ | 本轮只能提供机制依据与复用接口 | 下一阶段独立协议：扩队列、跨模型／任务、外部中心 | 三种子或数千格开发集实验等于通用性、外部验证或 NBE 录用证据 |

## 下周汇报的结构

每个问题汇报：问题与预先假设 → 对照及固定条件 → 全部正反结果和不确定性 → 当前能回答什么 → 尚需哪些外部证据。没有完成的数据明确标注进行中；数值实现验收、当前队列经验结果、推广主张分栏。

优先交付机制结论、成本与适用范围，不以最佳配置排行榜或实验数量作为主线。导师与用户的解释均视作需要核验的假设。既有线性方法、非线性方法及压缩对照保留价值；新增训练只用于填补明确证据缺口。

## 执行边界

仍使用 1264 训练与 296 开发参与者，不读取 test，不扩原始数据、不改标签；batch16、每 GPU 本项目不超过 10 GiB、双 GPU 独立派发、不影响 LOOK。保持原平台规则和父检查点。同一开发集既往参与任务与配置筛选，所有推断仍为探索性。

尚待确认的范围：收缩为代表性机制验证矩阵，或保留原全矩阵但取消最优选型。确认前不派发新的性能训练。归档和兼容性验证继续。
