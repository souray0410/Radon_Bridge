# 全网络联合训练纠正

用户指出主实验应全解冻；原计划Stage15把joint fine-tuning列为主要性能设置。之前把部分解冻配方沿用到主比较不符合当前目标。stage3是桥接位置，不能据此冻结stage1/2。

662e613的exp008_formal已主动终止：PID9431退出，exit143，0/30次完整试验，首baseline末状态epoch11；耗时1.737153分钟，结果不作为全解冻证据。原始日志保留ws。

全解冻设定：CFP/OCT stem至stage4、独立任务头、桥接压缩/线性mixer/展开参数全部requires_grad=True并进入优化器。BatchNorm affine和运行统计量均更新。Householder几何矩阵仍为固定buffer。无桥接控制使用相同全解冻与BN策略。

实现增加全参数优化器覆盖检查、逐模块可训练参数计数。新的验收验证batch4实际早期参数更新、全网络MHD/原生梯度一致、学习后的跨任务梯度抵达另一分支stem、BN统计更新及显存。

第一次验收在89ad58c失败：CUDA MaxPool3d反向不支持严格确定性。原池化深度窗口为1，a90bc4c已改成等价逐切片MaxPool2d；补充CPU前向/反向精确一致测试（含ReLU后并列最大值）。不关闭确定性，不跳过失败。

后续步骤：先同步池化修复并通过tests/check_full_finetune.py，保存验收输出后再冻结formal_full_protocol.json。正式新输出exp008_full_finetune，不续接已停止检查点；所有臂从相同预训练/随机头初始化开始。配置training_regime=full_finetune、batchnorm_policy=train、recipe_policy=user_full_finetune_override、非空recipe_override_reason，recipe在旧获选基础上id后缀_full、adapt_stages=[1,2,3,4]、training_regime=full_finetune。资格结果只保留架构/初始学习率选择的来源，不能声称已完成全解冻配方资格验证。

保留5种子3411–3415及6臂。20epoch是目标，先按全解冻实测step速度估算总预算；若不满足预算，预先统一减少所有臂epoch并说明，不能运行中按效果裁剪。预算至少扣9.603+1.738=11.341分钟，另对验收GPU用时向上预留1分钟，再按实测更新；本次008累计仍最多240GPU分钟。实际开始训练前每卡本任务<10GiB，allocator8GiB。

恢复半小时监控时优先查看本文件及最新运行状态，禁止重启旧exp008_formal或继续旧部分解冻协议。当前新全解冻训练未启动，修复后的数值/GPU验收结果待核实。

当前formal入口拒绝缺少full_finetune声明的旧协议，避免误重启部分冻结主比较。SSH连续超时，池化修复尚未部署；远端最后确认89ad58c，本地/GitHub修复已保存，需同步再验收。
