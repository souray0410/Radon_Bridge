# R&B：中心化拟合基补充实验

## 2026_09_05 最新追加：中心化拟合基配对补充

- 用户明确追加中心化与非中心化对应实验。沿用全部四类SVD桥（标准Radon、自身处理、空间打乱、线性重采样）×三个种子3416/3417/3418×ρ1/16、1/8、1/4，新增36项第二阶段训练，与现有36项非中心化SVD配对；加其余21项已有基线/可学习/QR，最终93项不同结果。
- 本轮唯一新变量是拟合基统计量：中心化使用E[xxᵀ]−μμᵀ，原版使用E[xxᵀ]。μ按训练集所有参与者、眼和空间位置逐通道统计，不是逐患者减均值。运行时两者均QᵀX，返回QΔ，不减/加均值；只比较中心化拟合，不声称检验完整中心化PCA编解码。
- 新增compression=fixed_centered_svd_channel；原learned_projected、fixed_svd_channel、fixed_random_orthogonal_channel保持兼容。保存完整基、均值、原始二阶矩、中心化协方差、方差谱和原始能量、来源及SHA。相同来源/种子各ρ共享完整基的嵌套前r列。ρ是维数比例，r16/32/64，每路h512/1024/2048。
- 不增加独立预训练网络。复用当前三个种子的独立最佳父检查点，各重新统计一次训练集得到中心化基（3项拟合）；核验训练参与者顺序、原始二阶矩、原生状态和父检查点SHA与原基一致。
- 主干6e-5、head/bridge1e-4、stage3/M32/S64、kernel3无偏置零初始化、batch16、AdamW/WD0.01/分支桥分别裁剪5、全参数与BN训练及原平台规则全部不变。每卡项目≤10GiB，GPU时长不限，LOOK和测试边界不变。
- 当前57项队列继续，不修改运行源码。隔离检出验证同步GitHub，排队等其报告完成且释放项目锁。拟合、最大ρ四模式前后向优化更新及完整128人/1264人诊断预检通过后才部署执行；失败/超上限/未平台需处理，不自动改变训练标准。
- 中心化36项与非中心化36项都用新诊断代码重新统计，共72项完整诊断。分开报告原始能量保留率、中心化方差保留率、初始与训练后统计，另保留残差及桥/主干梯度。只读诊断不改参数、BN、优化器或随机状态。
- 配对方向统一为中心化拟合减对应非中心化拟合；10000次参与者bootstrap跨种子共享索引；报告三种子均值/样本SD、逐种子值、3418单列、原两个种子参与筛选、CFP/OCT/平均、负结果、成本/轮次/显存/基与父检查点SHA。最终导师报告整合93项并做视觉审核。仍为开发集探索证据。


执行入口scripts/run_centered_study.py，汇总入口scripts/report_centered_study.py。固定中心化版本仍然是线性桥；拟合均值仅用于选择基，不直接参与运行时算子。
