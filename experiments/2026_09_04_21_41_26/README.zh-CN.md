# Overnight 参数与位置实验

固定共同的seed3416独立模态检查点，所有条件全参数联合训练至相同平台规则。已有无桥和stage3 M16/S64/rho1/8作为引用，不重复耗费GPU。

成对依次运行：stage2与stage2+stage3；M=8与32；S=32与96；rho=1/16与1/4。每次只有被考察轴变化，其余保持stage3/M16/S64/rho1/8。完整后按两分支macro-F1均值选最佳R&B设置；若预算允许，自动训练seed3417独立模态检查点，并做无桥/最佳R&B重复。

本轮最多60 GPU分钟，保守承接既有轮次上限73.341分钟，因此账面累计最多133.341，低于240总上限。每卡本项目进程合计不超过10GiB。预算不足时按上述顺序停止，不把未运行或未平台的条件列为完整结果。测试集不读；所有选择仍基于历史使用过的开发集，属于探索性证据。
