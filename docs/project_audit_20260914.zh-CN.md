# 两项目全链路审查：2026-09-14

这是一次范围、源码、部署和运行证据的审查，**不是全研究已经验收的证明**。现场截至2026-09-14 21:25（Asia/Riyadh）；后续以实时状态为准。已有健康训练和不可变科学源码未被本次审查中断或替换，未读取test预测。

## 审查方法与发现

对照已批准研究包，逐项区分：设计登记、实现、单元/结构验收、生产接入、真实输入全流程验收、运行和科学接受。检查了两项目活跃绑定、旧主线及新增feed、父模型目录覆盖、registry源码摘要、最近更新、运行中的宿主进度、GitHub准确提交CI、科学入口和验收/统计代码。

旧执行审计只读原生模型feed和主controller，漏掉LOOK机制和空间注册器；旧`remaining_implementation=[]`也不能代表新协议完成。本次扩展MHD_Models的只读审计：自动发现项目/补充feed与registry，核验spec及registry源码SHA，检查同run不同配置冲突、遗漏接线、失败及陈旧状态，分别显示外层case状态与内层宿主更新，展示合成验收的真实scope。已在Ibex独立快照通过全部227项CPU测试；其中15项专门覆盖审计。真实配置首轮未发现feed摘要/接线错误，但明确报告仍有实现与验收缺口。

**不会把receipt文件存在计为重新科学验收，不会把内层仍在更新的宿主误判为外层心跳停滞，也不会把没有case生成等同于整个完整矩阵已在排队。** GPU进程和Slurm step仍须现场另查；此审计不抢占、不领取、不申卡、不访问预测/权重。

## LOOK：逐包审查

| 研究部分 | 实际状态 | 验收与下一道门槛 |
|---|---|---|
| 有限父模型筛选、配方选择及三种子 | 控制器报告56/116接受；白内障ResNet50配对三种子已齐 | 控制器调用原训练acceptor；报告数不替代本次逐权重独立重验 |
| 原81宿主主线 | 已生成9个宿主case；21:23快照4个宿主有持续优化更新、5个尚无运行状态；完整case接受0 | 宿主训练→冻结→LOOK/原对照→整组统计与接受，不能拿父模型数作项目完成数 |
| 完整/缺失鲁棒继续训练 | 代码及依赖矩阵已部署；尚无此包真实训练结果 | 原case接受后自动生成；新增162次，原宿主与新宿主分开选优和拟合 |
| 蒸馏和CE学生 | 代码与去重规则已接入，尚在依赖等待 | 蒸馏162、去重CE54；教师冻结，train软目标，最终学生指标 |
| 逐层、打乱、受限写回、MLP、仿射机制 | 已注册自动生成逻辑，补充feed当前0 | 独立拟合、只读关闭与受限重拟合必须分开；MLP是对照，不能改变LOOK闭式方法 |
| 样本效率与成本 | 代码已接入；完整机制登记7182逻辑任务目前全待依赖 | 各子集重拟合统计，100%去重保留引用；逻辑任务不等于神经网络训练数 |
| 插值/平均池化/原分辨率PCA三路线 | 科学快照7d141fd、CPU与节点写回检查已过；registry和dispatcher已接通 | 81宿主/243路线视图，真实大队列GPU整组接受0；原分辨率PCA仍需512GiB allocation资源预检 |
| 单种子→其余种子 | 空间registry有3416三路线完整技术/报告接受门槛 | 不以分数高低放行；不能把这一机制说成所有旧研究包都采用单种子先行 |
| MMTM/EyeMoSt+/EDRL外部方法 | 适配审计/锁定边界保留，尚非全部可派发 | 不把二维改造当三维原论文复现；审计通过后独立锁定训练 |
| dev统计、统一test、迁移 | 对应代码有门槛；全研究正式接受与统一报告未完成 | 未到解封阶段；比较覆盖、全研究同时区间、模型/数据审计必须在test前完整验收 |

原主线与新增B/C训练制度不是相同配方，不能因学习率不同直接认定错误。每一匹配比较内部必须用自身锁定配方。原宿主的完整case接受是补充注册的依赖，目前不因周报期限绕过该门槛；可报告带状态的宿主进度，不能提前发表未齐对照的LOOK结论。

## Radon_Bridge：逐包审查

| 研究部分 | 实际状态 | 验收与下一道门槛 |
|---|---|---|
| 旧双来源父准备/主线 | 原父目录6/14；旧队列实际case0，旧计划201位置 | 等2D/3D匹配三种子；不能把旧controller空remaining当成完整新版就绪 |
| 完整三架构父模型 | 独立父目录28/73报告接受，额外native feed已接通 | 三维父仍是关键依赖；与旧目录可能重叠，不能相加当唯一模型数 |
| 三疾病共同队列、来源身份 | cohort与parent跨任务审计、标签/眼别/访视复核接口已实现 | 后续每个case实际父模型必须逐项验证；58358/12502是待相应manifest验收引用的共同队列口径 |
| 55臂同疾病同构、其余双网络12臂 | 矩阵/多来源构建与部分case执行已实现 | 完整新版生产case/feed与真实父模型重放、资源接受仍未全部接通 |
| 六网络729安排/27臂 | 完整逻辑登记及多来源构造存在 | 真实完整输入六网络GPU生命周期与多卡生产派发仍待验收，登记不算执行 |
| 32候选调参→公共配置→迁移 | 规则与位置清单存在 | 调参/选型/宿主case编译和接受链仍有实现缺口；不得只补一条桥就宣布完整 |
| 非线性宿主增强 | 双来源旧路径与多来源构建存在 | 完整新宿主三臂编译、资源和正式接受仍需衔接 |
| Ibex分解卷积补充 | c96d2cd构建适配经独立Ibex CPU检查；4d819be CI成功 | 尚缺正式case协议、队列、诊断/报告与真实输入GPU接受；目前不能说已经在Ibex跑 |
| ws02小队列分解补充 | 91beacf不可变科学快照真实运行；21:16队列报告16/24接受、失败0 | 原1264/296小队列，不计Ibex大队列结论；正式接受需检查平台和产物SHA |
| 几何×压缩交互 | 每档分解R有Radon/普通通信匹配 | ws02的24项没有新增SVD普通通信；须先验收历史匹配项，否则交互仍不可估计 |
| 来源关闭/自身/单向与分组 | 固定SVD/QR现有机制；分解构建明确拒绝直接套旧来源掩码 | 全局A/B混合来源，不能在隐空间K套块掩码就称删除原始来源边；分组方案仍需独立结构/资源/比较接受 |
| 正弦图、Fourier、梯度与残差 | 存在诊断代码和原队列证据 | 多来源/分解不能自动继承旧解释；相应真实模型视图必须重新验收 |
| 全研究统计/test/跨数据 | 部分组内报告存在，完整覆盖仍待接线验收 | 92526是去重前逻辑位置上界，不是已训练/排队数；分阶段锁定后才统一test |

生产旧双来源配置是microbatch1、effective_batch16；普通BN的更新不等价于单次batch16。完整研究文档要求匹配组在性能前锁定microbatch；迁移时要逐组明确，不能把梯度累积当作BN等价，也不能与ws02真实batch16混作同条件。此次未擅自调整正在运行或已锁定的科学配置。

## 共同验收与执行边界

- 精确提交CI已核查：LOOK c47be12（34877690170）、Radon_Bridge 4d819be（34877753483）均成功；生产科学版本另按各自绑定。CI不替代真实数据GPU生命周期。
- 两个旧runtime gate均明确是`ws02_synthetic_cuda_implementation_and_recovery_only`，并要求生产父重放与资源预检。保留这一范围，不能升级表述为完整大队列通过。
- 运行状态检查看到21份单卡RUNNING、3份单卡PENDING；这是账号作业快照，不等于24个科研任务，也不代表每张卡只训练一个模型。新申请仍遵循48小时及实际账户/资源准入。
- LOOK与Radon存在不同历史framework/source pin，不能为了目录统一热切换。新版本按严格输出/梯度/更新/恢复验收后独立部署；旧任务保留自己的来源。
- 训练/模型/拟合/评价视图分别计数，基础模型与项目分开。测试集入口、受限数据存储、声明的失败/未平台状态均不可为赶进度放宽。
- 本次未重跑所有模型的GPU训练，也未逐个重读全部已验收权重及原始数TB影像；这些属于真实acceptor/数据账本的独立门槛。尚缺证据的项目继续标为未验收。

## 修复优先级与交接

1. **已实施：**补齐自动审计的新增feed/registry、case内实际进度、验收scope和冲突检测；Ibex 227测试通过，随后安全部署只读检查入口。此修复不修改任何训练任务。
2. **本周主线：**继续LOOK已启动宿主，核实冻结/LOOK/匹配对照实际推进；三路线首个3416完整闭环通过再自动扩种子。完成Radon首套合格2D/3D父重放及参考匹配组。
3. **优先缺口：**将Ibex分解正式case/报告/资源接受与新完整case feed接通；保留SVD、普通通信和无桥匹配依据。不得使用小队列结论替代大队列证据。
4. **持续修复：**完整调参/宿主/六网络/全研究统计与test锁按已授权分阶段推进；不在新结果产生后改对比定义。所有未关闭项交由现有UKB自动维护任务续接，不能仅重复报告同一缺口。
5. **周报：**9月16日20:00核查方法实际执行，9月19日18:00冻结可汇报快照，9月20日08:30更新。以完整匹配组及反例/未完成项交付，不按矩阵数字充当成果。
