# 本周UKB执行安排：2026-09-13至下次汇报

用户确认：本周集中现有完整UKB队列；后续扩大数据、模型、任务仍保留。
统一使用dev称呼，首次解释为开发验证集。不更改原始划分、标签、历史文件。

## 执行顺序与资源

1. 现有健康基础模型训练继续。两项目CPU控制器分别每30分钟核验进展。
2. 各项目优先队列消费“已经锁定的父模型候选、原方案三种子重复、已就绪项目任务”。
   重复训练优先于普通未开始候选；项目任务就绪后优先派发。原任务ID、spec和原子领取不变。
3. 各项目调度器最多同时管理两份单A100 allocation；共享全账号24张申请上限与提交锁，
   其他资源继续既有基础模型任务。上限包含待批及身份未确认申请，不强行占满。
4. 每张卡最终约留10GiB；正式项目开始前完成实测资源验收。不修改健康worker源或科研batch。
5. 全量接受、配对齐全和开发重放均通过后才能完成项目研究单元；仅有基础模型成绩不能计项目效果。

## 初筛与正式资格

eligible_for_formal_selection=False是原初筛身份，不修改旧spec、不直接宣布正式选型。
获准的expanded_cohort_native_screening模型可以进入有限项目候选清单；选择只提名原生配方。
随后必须完成种子3417/3418、完整模型/BN/Node ID和预测重放、数据配对及资源验收。
未通过者保持待处理。已锁定候选的初筛分数不能冒充独立确认性证据。

## 下周汇报必须分开回答

- 基础模型：验收数、架构/任务/模态、配置与原生dev结果。
- 项目方法：同一宿主/父模型下的匹配增益，逐种子、失败及反例。
- 方法对比：具体论文方法与受控模块适配分开；三种骨干不等于三个不同比较方法。
- 扩展准备：LOOK其他眼科队列；Radon_Bridge跨器官与1D/2D/3D场景先调研，不据未验收数据开跑。

当前LOOK自动执行的是同一宿主结构上的三骨干/三位置及修正对照；不能称已完成三个
外部论文宿主的A与A+LOOK比较。该项需要独立方法审计与协议，列为本周明确交付缺口。
Radon_Bridge已有MMTM/注意力单stage适配及继续训练/桥/普通通信增强对照，仍不称完整医学SOTA复现。
