# 2026-09-20导师汇报：两项目阶段成果执行安排

制定时间：2026-09-14，Asia/Riyadh。9月20日为周日。本文件调整执行优先级和阶段交付，不撤销已批准实验，不更改训练/选优/数据协议，不把期限当作科学完成标准。

## 核心交付

本周以UKB眼科项目方法结果为核心。基础模型数量、CI通过、GPU数量不代替LOOK或Radon_Bridge的匹配方法结果。优先完成一个完整参考匹配组，再按相同规则扩展；729种六网络安排等完整研究仍保留，不承诺本周全部完成。

- LOOK：目标首先获得一组疾病×架构×融合位置、三种子的完整宿主及LOOK/既定匹配对照结果。完整输入、缺CFP、缺OCT各自报告宿主最终macro-F1、LOOK增益、逐种子波动和成本。其他位置/疾病/架构按已批准顺序继续，不按早期效果筛掉不利组。
- Radon_Bridge：目标首先获得一组疾病×架构、三种子的无桥继续训练、SVD-Radon、同基同宽普通通信、自身处理、MMTM、交叉注意力六臂结果（18个训练位置；严格匹配项可复用）。保留2D CFP和3D OCT、独立任务头及分支平均主指标，不将2D OCT替换3D以赶进度。
- 第一个参考组按已锁定候选及依赖验收就绪时间进入，使用固定组键破同分；这是执行次序，不是按新方法表现选组。尚不能选定时列候选与阻塞，不预称完成。
- 三种子未齐或对照未齐时可展示明确标注的运行状态、单种子探索结果，但不计入三种子完整匹配交付。

## 日期与验收门槛

| 日期（Saudi） | 主要工作 | 可核验产物/逾期动作 |
|---|---|---|
| 9月14–15日 | 逐项定位父模型缺口；核对三维数据、原生训练、完整模型加载；补最短参考组的执行链路 | 父模型/任务依赖清单、真实输入预检与恢复记录、代码CI及部署SHA；列出尚未接通步骤 |
| 9月16日20:00前 | 检查两项目首个参考组是否进入正式方法执行 | 若仍无方法任务，主动报告阻塞证据、已采取修复、下一可执行动作和时间风险；不能继续只汇报模型数 |
| 9月17–18日 | 优先完成匹配组和三种子；随后扩展既定对照与机制 | 逐臂dev预测/模型/来源验收、停止规则、分支或缺失状态结果；每组完成即统计 |
| 9月19日18:00 | 形成汇报候选快照，复核接受记录、统计与老师问题 | 冻结一次有时间戳的汇报数据快照；训练继续，未完成项单列，不提前停训 |
| 9月20日08:30 | 刷新已验收结果，形成周日阶段汇报 | 两项目分别总结：完成、结果、反例、成本、局限、下一步；当天具体会议时间尚未另确认 |

这些是推进目标及检查时间，不是保证某天训练收敛。遇到无法在周日前完成的关键依赖须在周中主动说明，不能等汇报时才披露，也不降低完成标准。

## 执行优先级

1. 修复/验收阻断眼科参考组的代码、数据、资源与接受接口。
2. 完成参考组所缺的原生父模型和3417/3418重复；Radon三维路线为明确阻塞项。
3. 正式项目训练、LOOK拟合、匹配对照、必需诊断、重放和统计。
4. 扩大到已批准的其他眼科实验组及必要父模型。
5. 独立基础模型已有有限候选训练。新分组卷积、无空间缩放PCA等补充先完成设计/验收，不挤占尚未完成的核心参考组；没有资源/算法验收不得自动训练。
6. 心脏数据传输、校验和任务盘点以CPU/I/O为主；独立心脏基础模型需数据/标签/划分另行验收，心脏项目方法实验在眼科阶段验收后开展。

## 资源安排：配置与实际分开

9月14日现场24张单A100运行。原分配为LOOK2、Radon_Bridge2、独立model20；项目卡当时运行的是父模型，项目方法接受数均为0。

现有v1角色策略已调整未来准入：LOOK预留6、Radon_Bridge预留6，独立model最多12，全账号上限仍结合实际Slurm限制与当前24张配置。共享提交锁内验证后原子写入，保留旧策略及SHA；不取消任何现有allocation，不停止健康模型。旧任务自然释放后，新的项目申请按就绪候选获得名额。项目通道优先项目任务，也执行该项目必要父模型。

不能把预留6张写成目前已分配6张。未来若就绪方法任务足够，可在验收后进一步向项目倾斜并保留至少4张model资源目标；本次尚未部署全动态v2策略。未知资源先测完整峰值，新增申请仍48小时。数据迁移不占GPU。

## 本轮现场基线（有日期快照）

- LOOK旧线上筛选50/116候选接受，0方法任务接受；9组均显示等待配对三种子。此分母不是378项补充研究的完成数。
- Radon旧筛选6/14，0方法任务接受；完整父准备25/73。九个三维来源组当时均0接受；此分母不能与旧筛选相加。
- 新完整研究还有任务编译、真实输入/多来源资源验收、执行接受与统计衔接缺口。不能将旧status的remaining_implementation空数组解读为完整方案已上线。

## 回答老师的问题

| 问题 | 本周提供的证据 | 不越界的解释 |
|---|---|---|
| 基础模型之后具体做了什么 | 完整任务流水、父模型→宿主/桥→接受结果映射 | 模型训练不等于方法实验 |
| LOOK有没有必要 | 同宿主加/不加及输出/既定修正对照，完整/缺失分别评价 | 无完整匹配则不能宣布改善 |
| Radon是否超出普通通信 | 同基同宽Radon/普通/自身对照及非线性适配 | 不把某个最高分概括成普遍优势 |
| 能否增强已有方法 | 既定宿主继续训练/加模块/加普通通信的进度和接受结果 | 未完成不称已证明；不为周报临时省掉继续训练对照 |
| 为什么早期只有千人 | 原筛选脚本、病例平衡/冲突/配对/质量排除的逐步流转记录；当前大队列对照 | 不把所有排除解释为质量差，不以总文件数替代人数 |
| 下一步如何推广 | 眼科→心脏MRI/心电的输入、标签、配对与迁移接口盘点 | UKB跨器官不是独立外部数据集 |

## 数据范围更新

眼科既有原始档案5,178,813,443,447 bytes；标签来源6份100,808,628,023 bytes复用。新增仅5组：静息心电20205、运动心电6025.zip、心脏MRI20207/20208/20209，共8,360,335,258,048 bytes。合计原始资料约13.64TB，不含缓存、解压及模型。
DXA20158、胰腺20202、肝脏20203.zip及颈动脉20222/20223/20226.zip暂缓，保留源盘。传输与字节校验不等于器官标签/访视/配对语义验收。

## 统计和展示

本周保持dev评价，test不解封。沿用各研究已批准的配对bootstrap及比较族；图示逐种子和缺失状态/分支，标清样本数、指标方向、来源、配方和训练完成状态。不能因期限更改batch、BN、学习率或平台规则。周报日期快照不停止健康训练。

## 自动执行边界

既有远端控制器、原子领取、断点保护和三小时审计继续。三小时检查同时推进一个阻塞参考组的可独立验收实现，不止巡检；发现未进入方法训练、依赖停滞或代码未接通需纠错。已运行内容、准备代码和未实现计划分开记录。
本文件是执行排序和验收目标，不能单凭本文件称整个扩大研究已全自动。新增执行环节须测试、准确SHA CI、Ibex真实验收、安全部署后才能更新上线状态。

## 2026-09-14后续确认：分阶段投稿

用户确认12月31日前正式投稿，允许长期大矩阵和首篇实验包分阶段。见[学期交付安排](semester_delivery_2026.zh-CN.md)。阶段test需另外锁定完整研究包并实现验收入口；当前仍不解封test。原完整范围保留，不再要求所有长期探索成为首篇投稿前置。
