# 通道压缩：SVD、随机QR、可学习映射

本页登记核心机制缺口，未完成的行不填零、不排名。先在ws02同一小队列、3416、同父模型和原停止规则下完成有限匹配组；Ibex49机制范围保留。

|通道处理|Radon|普通线性通信|问题|
|---|---|---|---|
|固定非中心化SVD，rank32|已有接受结果，核验后复用|已有接受结果，核验后复用|作为训练数据驱动的固定方向基准|
|固定随机QR，rank32|待本包预检/训练|待本包预检/训练|收益是否依赖SVD方向，而非任意正交子空间|
|可学习通道编码/解码，rank32|待本包预检/训练|待本包预检/训练|更新通道映射是否优于固定映射；额外参数必须承认|

QR按独立种子随机矩阵正交分解构造，不按特征能量排序。可学习映射初始使用同一QR方向，训练中更新，不能声称参数量与固定方案完全相同。保持Stage3/M32/S64/k3、真实batch16、相同父检查点和开发集选择，沿原min8/max60/patience6规则训练。

预先登记七个差值：SVD−QR、SVD普通通信−QR普通通信、SVD−可学习、SVD普通通信−可学习普通通信，以及每种通道处理下Radon−普通通信。完整六项后给出10,000次参与者配对bootstrap普通与七项同时区间；开发集选择及单种子限制保留。不能由SVD能量保留直接推导分类一定更好。

中心化SVD是“是否需要减去训练特征均值来确定方向”的独立问题，需要配对训练统计验收；分组卷积是连接结构问题；二者保留后续，不能混成这三种通道处理已全部覆盖。老师的A/A+桥也继续作为独立优先任务，不被本包替代。
