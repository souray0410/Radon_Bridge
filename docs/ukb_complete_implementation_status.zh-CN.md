# UKB完整研究实现状态：2026-09-14

本分支是准备与验收中的实现，不是已上线的完整研究队列。现有生产工作流继续使用自己的不可变源码；不得因读取本分支而切换活跃worker。

已准备多来源完整MHD网络、独立标签与头、固定/可学习通信、来源设备放置、训练恢复、匹配组统计及诊断、有限父模型清单和92526个逻辑位置登记。逻辑位置不等于新增训练数，也不等于可执行任务。

最新独立ws02检查：全部45项单元测试通过（14项第三方弃用警告），结构检查通过。此前实际ResNet/DenseNet/Swin小输入、六来源及双GPU恢复验证保留在独立验证目录。这些结果说明相应实现检查通过，不替代真实完整输入的GPU资源验收或科学性能。

正式完整队列仍需：

- 完成新父模型验收、原开发预测严格重放及跨任务参与者使用审计。
- 验收完整输入的六网络资源、匹配组microbatch锁和最少卡数放置。
- 衔接调参、公共配置选择、外部宿主case编译和执行接受记录。
- 衔接多卡Slurm派发、动态需求策略和全研究比较/全局统计。
- 只有模型、对比和数据审计全部锁定后才能开启test。

新只读诊断修正了关闭残差通路时返回值的语义；历史诊断不覆盖，需在新版本下重新核验相应视图。科学训练输出不据此改写。

用卡及器官扩展次序见[统一扩展边界](ukb_multiorgan_scope.zh-CN.md)。当前眼科项目优先；其他器官数据验收后的独立基础模型可以提前训练，其他器官方法实验随后开展。
