# 2026-09-17：独立实验臂与阶段交付

本轮为用户批准的“双线推进”实现，科学矩阵、数据、父模型选择、batch、BN、种子、损失和test封存不变。通用准则见 `workspace/DELIVERY_STANDARD.md`，PHD为共同规则入口。

## 实现与计数

`studies/project_units.py` 将同一case拆为共享准备、各实验臂、核心报告、完整报告。六臂case对应9个执行单元，55臂case对应58个；它们不是9/58次新增训练。原case与arm身份计算保持不变，不复制父权重或重新创建科研种子。

共享准备完成后六个核心臂独立就绪；宿主增强仅等待对应宿主。每个unit由现有Claims领取，有独立状态和暂停目录。科学权重仍在原case/arms内。共享run锁排斥旧整case执行器，unit锁拒绝同一单元并发，报告锁避免核心/全包同时写诊断。重复种子需本项目周包验收以及本组3416核心包验收，缺政策不放行。

核心包只覆盖continue、svd_radon、svd_resample、svd_self、mmtm256、attention256。核心完成不写全case接受标记，其余49臂保留。完整报告还需所有实验臂和既定诊断。已完成核心诊断按原checkpoint身份供全包复用；图源与图片SHA一致时不重新渲染，避免补充包改变已验收核心图文件。统计族明确为该核心包dev，不冒充全研究同时区间。

`analysis/package_rollup.py` 按完整核心包累计发布同一current入口。内容寻址、原子切换、重复不增行、失败保留旧版；未完成包显示状态，无零分占位。逐臂值、核心包区间/图及全case统计分别保存。

## 实施验收状态

- 本地Python3.11、torch2.8、锁定MHD V4：完整单元测试85项通过；新增用例覆盖独立就绪、宿主依赖、配置未变、核心不冒充全包、伪造/缺失验收、原子累计发布与三条架构配对。
- 结构/导入/源码锁检查通过。上述均不是Ibex生产资源验收，也不是科学结果。
- 本轮Ibex SSH曾返回连接重置/超时；随后域名恢复登录到vsc509-29-r，Slurm可查询，但该节点/ibex/project为空且/ibex/user/mengh不可见。仅SSH恢复不代表项目存储恢复；没有停止任务、改远端快照、申请新卡或访问test。上次远端核验仍是上一次有日期的记录，不能据此更新实时健康状态。

## 待连接恢复后的唯一接续路径

1. 读取active workflow、项目/基础模型claims、Slurm、进度和日志；确认当前owner及是否已被其他维护更新，不能照抄旧job ID。
2. 以本次commit部署独立管理/科学快照；原健康任务继续用其源码。完整数据、合格父模型及实际框架锁必须匹配。
3. 在已分配且通过资源准入的Ibex步骤完成真实pair严格加载、前后向/更新、恢复、独立臂共存与报告链路验收。未验收父模型仅用于隔离工程预检，不能生成正式父模型接受标记。
4. 验收回执须含 `execution_contract=radon_independent_units_v1`；旧整体case的accepted字符串不足以启动新版。将回执SHA绑定既有parent controller的project配置，绑定有限周包政策和原dispatcher队列。保留至少2张通用模型资源，新增申请48h，禁止竞争性owner。
5. 编译正式队列，观察真实领取、训练/基拟合进度、断点及核心报告触发。只有这些证据取得后标记deployed/running/accepted。

未关闭：远端连接、实际3D父模型验收、Ibex独立单元资源/恢复/下游接续、当前周包精确绑定。该清单不授权改变科学标准或跳过父模型候选选择，也不声明“全自动已上线”。

## 11:18 UTC现场补充

通过原节点vsc509-03-l恢复项目存储访问，独立radon_46cd282快照在Ibex通过15项针对测试（未做正式GPU项目验收）。计算节点的DenseNet3D父模型worker仍运行；完整父模型控制器尚无project绑定。新逐臂执行代码已同步main，但未激活正式派发。登录管理进程消失后的领取、待批申请owner接续须优先核对；健康计算worker及allocation owner未停止。现场证据在OPS/independent_delivery_20260917。
