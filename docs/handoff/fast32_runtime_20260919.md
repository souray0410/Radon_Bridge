# Ibex 32层父模型：已进入正式训练，尚未完成验收

现场核验截止：2026-09-19 02:13 UTC。这是运行进展，不是新增性能结果。

## 现在确实在做什么

青光眼、种子3416、ResNet18-3D，OCT在完整体积跨度均匀取32层，每层224×224；真实batch为16名参与者（最多32眼）。保持原训练集58,365人、开发集12,503人和50轮停止规则，不访问test。canonical run为`2026_09_17_20_04_14_937028`。

原有限owner获批后完成线程2的全训练数据读取、全开发集、保存与下一更新精确恢复预检；accepted receipt SHA为`883d08ada6da6bd0547cec9e5775338acd8ac18b45bce7ca9dc9b19070ac1afc`，三份预检断点SHA独立复核一致。预检约1,796秒，GPU峰值37.40 GiB、step内存峰值32.02 GiB。

随后正式训练从93推进到147次参数更新，仍在第1轮，已处理2,352名参与者，真实last.pt已写出。正式GPU保留内存峰值约47.0 GiB，高于短预检，故继续按整卡余量监测，不能拿短probe代替全量监测。当前80 GiB卡仍有约32 GiB余量。父模型未训练完成，尚不能派发正式大队列六臂。

## 本轮发现和修复的故障

辅助控制器在领取额外任务之前因缺少训练人数配置`train_sizes`退出，主训练正常推进。已用原部署的SHA校验工具从不可变训练清单补齐人数，生成独立新配置，确认原锁空闲后以同一控制器输出目录和任务槽锁恢复。实际状态已进入稳定基线采集，额外任务仍为空，主训练推进至147更新；没有停止或重训主任务。

配置生成器修复已版本化为[MHD_Models 6fbaf28](https://github.com/souray0410/MHD_Models/commit/6fbaf2804cb75906b8fd00b8ae0de5dad0c3942e)，Ibex独立CPU快照9项针对测试通过。**当前辅助控制器恢复已经核验；未来pending-owner快照尚未切换此生成器**，后续接续前必须单独验收，不能把代码合并当作全部部署完成。

原128层DenseNet父模型仍保留暂停断点（15,319更新）。本次没有替换其配方或断点。下一检查关注正式更新/保存与资源峰值、辅助控制器稳定性，以及未来owner配置绑定；父模型接受和六臂比较继续分别把关。
