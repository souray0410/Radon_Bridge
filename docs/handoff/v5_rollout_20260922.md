# V5 候选迁移交接（2026-09-22）

当前是候选源码，不是正式发布或生产迁移完成。历史科学结果与其原始框架版本保持不变。

框架固定为 `c78cfbdc9ec01e4dc1dc8ee04d04b2e6a8446808`，MHD_Models 固定为 `b924a189ceb768310feaa0240adc95b51a33d07e`。本分支采用 V5 当前格式，正常加载拒绝旧格式，不提供 V4 回退。旧资产需独立转换、数值回放和新身份登记后才能进入正式运行。

完整 CPU 单元测试 182 通过；后续调度修复的 runtime 子集 82 通过。 CPU 通过不替代 GPU 完整流程、资产迁移或生产验收。

六条 Ibex 历史 RAM 峰值超限事件尚未关闭，待独立配置验证及实际续训；不得删除历史峰值。

## 安装运行依赖

在独立虚拟环境完成本项目及锁定框架安装后，从已授权、精确 checkout 的私有 MHD_Models 安装运行组件：

```bash
python scripts/install_operations.py --source /authorized/MHD_Models --check
python scripts/install_operations.py --source /authorized/MHD_Models
```

安装器核对 commit 与 Python 源码树摘要，不自动获取私有仓库、不使用旧目录回退；其成功仅表示安装一致，不表示数据/训练验收。公开项目的 CPU CI 不获取私有源码；正式调度、原生训练及 GPU 准入依赖上述组件。

## 后续验收

取消固定 10 GiB 显存预留，按设备实际占用、完整生命周期峰值和并发预算准入，host RAM 规则保留。剩余历史配置和逐项目单卡流程仍需核实。先在独立快照验证迁移，再在完整更新边界交接，保护健康 worker 和 allocation owner，不热改其绑定源码。Ibex 继续由现有系统维护 24 张运行与排队总预算及释放后的轮流补位。

未完成：实际依赖资产清单、旧资产一次性转换、完整恢复及跨平台更新对照、ws02 单张 32 GiB 全流程、生产切换和原事件关闭。正式 V5 tag 与发布资产尚未替换。

## 单卡有界流程验收

合成 2D/3D 数据、39 类机制和精确下一次更新；不是完整研究运行或真实父模型迁移验收。已在单张 RTX 5000 Ada 上通过；[完整范围与结果](../acceptance/v5_bounded_workflow_20260922.json)。LOOK 检查训练/精确恢复、PCA/修正拟合、重载、评价和报告；R&B 检查机制梯度、mask 及精确下一次更新。原失败环境缺少依赖的日志保留；补齐声明依赖后使用独立输出重跑，无阈值放宽。

## Ibex 资源恢复派发器交接

独立历史执行修复快照 radon_resource_106_v2 已在 Ibex 激活：106GiB worker、128GiB allocation、2GiB owner；六项原峰值约104.017–104.019GiB，未改数学配方。原管理器经 PID/启动身份/无子进程检查后退出，新管理器复用原 requests/dispatcher锁与 Claims，现为6项受控恢复候选、workflow_allocations_active、error=null。9项原子恢复检查与7项资源/领取检查通过。

旧故障 claim 和状态保持封锁，只有新合格 owner 能核验原断点、终止步骤、证据SHA后原子领取。暂停后再封锁，防止旧104GiB owner重领；一次审核仅放行一次尝试。健康GPU任务与allocation owner未取消。等待正常释放后的现有轮转，尚无新正式更新证据，六项事件继续开放。
