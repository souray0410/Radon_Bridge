# V5 候选迁移交接（2026-09-22）

当前项目源码仍是生产切换候选；依赖已固定到正式发布的 MHD V5。历史科学结果与其原始框架版本保持不变。

框架正式 `V5` 固定为 `1287681c08846e11364c81653048435482e772a7`，发布 wheel SHA256 为 `c022b4f4b0fa1f29458ad1bf9e0d04f6773e9454ab3bd8c07d416294483aab48`；MHD_Models 当前固定为 `cc16e74a8cfc705d69b3d31efe2daeec9404471f`。本分支采用 V5 当前格式，正常加载拒绝旧格式，不提供 V4 回退。旧资产需独立转换、数值回放和新身份登记后才能进入正式运行。

完整 CPU 单元测试 182 通过；后续调度修复的 runtime 子集 82 通过。 CPU 通过不替代 GPU 完整流程、资产迁移或生产验收。

六条 Ibex 历史 RAM 峰值超限事件尚未关闭，待独立配置验证及实际续训；不得删除历史峰值。

## 安装运行依赖

在独立虚拟环境完成本项目及锁定框架安装后，从已授权、精确 checkout 的私有 MHD_Models 安装运行组件：

```bash
python scripts/install_operations.py --source /authorized/MHD_Models --check
python scripts/install_operations.py --source /authorized/MHD_Models
```

安装器核对 commit 与 Python 源码树摘要，不自动获取私有仓库、不使用旧目录回退；其成功仅表示安装一致，不表示数据/训练验收。公开项目的 CPU CI 不获取私有源码；正式调度、原生训练及 GPU 准入依赖上述组件。

## 后续验收

取消固定 10 GiB 显存预留，按设备实际占用、完整生命周期峰值和并发预算准入，host RAM 规则保留。剩余历史配置和逐项目单卡流程仍需核实。先在独立快照验证迁移，再在完整更新边界交接，保护健康 worker 和 allocation owner，不热改其绑定源码。Ibex 继续由现有系统维护 24 张运行与排队总预算及释放后的轮流补位。

未完成：A100与完整UKB流程、生产owner切换和原事件关闭。正式V5依赖pin及代表性发布wheel回放已完成，不代表这些生产门槛完成。

## 单卡有界流程验收

合成 2D/3D 数据、39 类机制和精确下一次更新；不是完整研究运行或真实父模型迁移验收。已在单张 RTX 5000 Ada 上通过；[完整范围与结果](../acceptance/v5_bounded_workflow_20260922.json)。LOOK 检查训练/精确恢复、PCA/修正拟合、重载、评价和报告；R&B 检查机制梯度、mask 及精确下一次更新。原失败环境缺少依赖的日志保留；补齐声明依赖后使用独立输出重跑，无阈值放宽。

## Ibex 资源恢复派发器交接

独立历史执行修复快照 radon_resource_106_v2 已在 Ibex 激活：106GiB worker、128GiB allocation、2GiB owner；六项原峰值约104.017–104.019GiB，未改数学配方。原管理器经 PID/启动身份/无子进程检查后退出，新管理器复用原 requests/dispatcher锁与 Claims，现为6项受控恢复候选、workflow_allocations_active、error=null。9项原子恢复检查与7项资源/领取检查通过。

旧故障 claim 和状态保持封锁，只有新合格 owner 能核验原断点、终止步骤、证据SHA后原子领取。暂停后再封锁，防止旧104GiB owner重领；一次审核仅放行一次尝试。健康GPU任务与allocation owner未取消。等待正常释放后的现有轮转，尚无新正式更新证据，六项事件继续开放。

当前 companion 已移除无现役调用的旧 packed_review（固定10/20GiB与独立控制账本）；冻结历史执行保持原源码。新工作沿用已有的 measured prepared_owner/controller 与共享Claims。原b924回放证据仍保留其原执行SHA。

成对原生训练选优状态与诊断读取开始统一使用当前V5标识、研究identity、epoch和Node IDs校验；完整续训文件不能充当选优权重，旧文件须独立转换。当前改动未覆盖全部cohort、augmentation和host派生产物，不能据此宣称整个项目契约闭环。

57c64a5的190项完整CPU检查及CI35752850275通过。当前单卡39家族原生梯度、mask及精确下一更新集成在独立源码中再次通过；真实生产资产/全数据/诊断入口还需各自验收，不能用该集成代替。

候选资源入口进一步统一：共享包按设备实际占用和显式任务预算设置 allocator，不再隐式继承旧工作站上限。当前 Git 锁已更新，正在执行的远端验收继续保留启动时的旧精确快照；其结果不冒充新入口的部署验收。

### Deployed attention and CMX source coverage, 2026-09-22

Merged the exact deployed lineage ending at `28ebefcb9136e5fe1e56fc1d102130c288203670`, including the cross-attention and CMX FRM host/augmentation routes. Historical scientific reports keep their original evidence dates. Current PilotGraph checkpoints use `radon_pilot_state_v2`, with explicit roles for selected states, native parents, full resume, profile recovery, stopping/interrupted states and augmentation hosts. Current trainers, cohort replay and dependent analyses reject unconverted files. The full resume includes the selected state; epoch0 selection remains valid. A stopping/interrupted snapshot is not represented as exact resume state.

The old attention/cohort fixed10GiB reserve and 9/10GiB profile ceilings are removed. Bounded exclusive qualification acquires the existing physical-GPU lock, rejects non-cooperating compute owners, measures free memory and retains15percent host headroom. Shared admission still belongs to the existing scheduler. CPU207 tests passed, including exact AdamW next-update recovery, legacy/wrong-role rejection and ownership-lock failure cases. Layout and terminology checks passed. Actual PilotGraph asset conversion, full replay and dependency-reference migration are still open; no production queue is switched by this code change.

Offline PilotGraph conversion is implemented separately in `studies/v5_pilot_migration.py`. It requires hashed source descriptions, exact Node IDs and shared-parameter aliases, matching optimizer class/group order, strict module/parameter/buffer mapping and completed-epoch history/RNG. It preserves all original payload values and scientific identity, writes to a separate locked directory, and marks results awaiting numerical replay with dispatch disabled. It does not migrate dependency references or claim acceptance. CPU full suite:219 passed, including12 conversion checks; actual core-six source audit is in progress on CPU against the original pinned V4 source.

### Current configuration revisions

The offline `studies.v5_cohort_revision.revise_parent_references` tool creates one
atomic best/resume/configuration revision from converted states and accepted V5
native parents. It preserves research run/case IDs and all numerical training
state; current configuration and resume identity receive new hashes. It verifies
parent conversion and replay receipts, rejects changed source/seed/branch or an
inconsistent selected/resume boundary, and leaves dispatch disabled until the new
reference path is replayed. Host-augmentation references require their own complete
migration and are explicitly rejected by this native-parent-only operation.

原研究run/case身份保留，当前配置直接引用V5父资产并重新计算内容哈希；旧配置身份只写入溯源。
离线工具不会放行未完成的宿主迁移，不改变训练张量、优化器、随机流、选优或历史，也不向普通加载器增加V4回退。


2026-09-22追加：19个唯一当前配置的严格单卡profile已独立接受（覆盖21个含复用的条目）：初始化、5次预热、20次更新、完整296dev、保存重载及下一次更新一致；原生梯度容差1e-5/1e-6。最大allocator reserved 7.87890625GiB，进程显存采样最大8.30078125GiB。物理显存是阶段采样，不宣称连续捕获全部峰值；这些小队列结果不认证Ibex大队列或全项目完成。 详见../acceptance/v5_strict_profiles_20260922.json；生产切换仍未完成。

离线augmentation宿主转换器补齐：从已接受当前父引用的选优模型，重建单一V5宿主清单；保持原模型、选优epoch和预测，原训练标记V4。加桥配置修订同时更新父引用和宿主引用；未提供显式映射仍拒绝。247项CPU通过，三种真实宿主及九个加桥配置的当前路径回放尚待完成。


2026-09-22追加：三种宿主和九个加桥配置的当前父/宿主引用已独立接受：正常V5 build，296人初始化宿主预测及选优预测，两次原16批量更新、原生梯度/状态及当前断点下一步完全一致。仅保留原科研结果；原来源、Tensor和选优epoch不变。证据docs/acceptance/v5_augmentation_references_20260922.json。factorized/centered、A100、完整下游及生产切换仍未完成。

2026-09-23 centered-SVD 当前代码检查发现 `cohort_centered.fit_basis` 仍单独要求 20 GiB 空闲，其中包含已取消的固定 10 GiB 显存预留。候选代码改为复用同仓库 `runtime.exclusive_gpu.acquire`：核对唯一显卡、物理 GPU 锁、其他 compute owner、实际空闲量及 15% 主机内存；拟合本身继续调用基于实际占用的 allocator 配置。CPU 247 项通过；此改动只修正未来当前入口，不修改冻结的历史科研执行或其产物。当前 centered 资产的 V5 转换、GPU 完整峰值、正常读取路径回放和生产切换仍待验收。

2026-09-23 Ibex 现场核对：Slurm 帐户 24 个 GPU 请求槽已占满（11 RUNNING、13 PENDING）；现有轮转器与 R&B/LOOK CPU owner 状态新鲜且 `error=null`。R&B 六项内存事件的 `resource_admission` 显示 ready=6，但六个原 run 的 `operational_hold` 尚在，历史/断点无新正式更新证据。因此只证明等待与派发准入持续运行，不关闭六项故障；不新增申请或取消健康 owner。

2026-09-23 剩余历史资产核查：factorized 包 24 项均有选优、完整续训、接受凭证，使用 3416/3417/3418 三组父权重；centered 包 2 项同样完整，使用 3416 父权重及两份中心化基。两包 26 项原 V4 代码与科研资产已独立完成只读来源描述审计。发现当前 V5 正常构图漏掉合法的 `factorized_projected` 变体，候选代码按原数学定义恢复全局 `BKA` 因子卷积、配置校验、参数和 buffer 名称；不更改原资产。原算子七项针对测试及项目 254 项 CPU 单元测试通过。

随后 26 项的选优与续训状态共 52 份完成独立离线转换和逐项真实数据回放。每项在原 V4 与直接 V5 下比较 296 人开发集输出、两次原始 16 人更新、八个中间状态、输入与参数梯度、优化器、buffer 和断点下一次更新；FP32 使用 `rtol=1e-5, atol=1e-6`，两套独立 reviewer 均接受。factorized 24/24 的 V5 allocator reserved 峰值为 8.64453125 GiB，centered 2/2 为 8.15625 GiB。完整脱敏摘要见 [接受凭证](../acceptance/v5_factorized_centered_20260923.json)。这是单卡 Ada、开发集范围的迁移诊断，不是完整 UK Biobank 协议、A100 验收或生产切换；旧科研资产未修改，当前依赖引用等待正式 V5 发布身份后再修订。

正式发布后，项目 submodule、源锁和当前交接引用统一固定到 `V5=1287681c08846e11364c81653048435482e772a7`，私有运行 companion 固定到 `MHD_Models=cc16e74a8cfc705d69b3d31efe2daeec9404471f`。候选 `c78cfbd` 与正式框架提交的 `src/mhd_framework` Git tree 均为 `1f9cc4184f5bd106493a0333fa6f98d8ebd5ce33`；companion 的候选 `228b125` 与正式 pin 的 `src/mhd_models` tree 均为 `e3bc9d830d36f83da7bc1b9f7605e30184d6dcc0`。因此既有候选运行证据保留实际提交身份，不回写历史。另从 GitHub 重新获取正式 wheel 并核对摘要，在隔离 ws02 单卡用最大 factorized 配置完成 V4→正式 V5 的296人dev、两次原16更新、梯度、中间状态、优化器/buffer和断点下一步回放，独立 reviewer 接受；见[正式发布pin凭证](../acceptance/v5_formal_release_pin_20260923.json)。该代表性回放仍不代替A100、完整UKB协议和生产owner交接。

### Ibex 六项主机内存事件复核，2026-09-23

六项完整 UKB 原生训练仍均为 `needs_review`，对应 claim 均为 `failed`；旧 step 已终止，断点之后没有新的正式 update。106 GiB worker、128 GiB allocation、2 GiB owner 的受审恢复配置和 standby owner 仍有效，并复用原 request journal、dispatcher lock 与 Claims；现场有九个绑定该恢复配置的请求等待既有轮转，但尚无恢复 owner 启动。另三项在途训练的 claim 与状态持续更新，未取消、未热改。

这六项不是 296 人迁移诊断：其哈希核对后的完整 train manifest 各含 58,365–58,403 个样本，development manifest 各含 12,503–12,510 个样本，且 `test_used=false`。六项原断点均明确属于 V4。当前 V5 正常入口继续拒绝这些未转换断点，不把私有旧执行修复当作 V5 兼容路径。生产交接仍需逐项离线转换完整续训状态、生成不可变 V5 spec/资产身份、A100 完整 train/development 回放、更新边界的原子 claim 交接，以及一次新的正式 update 和全部下游引用核验。脱敏复核见 [验收记录](../acceptance/ibex_memory_recovery_audit_20260923.json)；事件未关闭。

### 首个完整 UKB optimizer-boundary 静态转换，2026-09-23

从一项仍健康推进的完整队列 DenseNet3D 训练中只读复制了稳定断点；原 owner、claim、checkpoint 与 spec 未改。一次性 converter 为合法 scheduler namespace 迁名增加显式 adapter，且同时绑定旧/新 class identity、两份源码 SHA 与完整 state 字段；未知类型或任一变化均拒绝，正常 V5 loader 不增加 V4 回退。

隔离 CPU 转换与独立复核逐值保留 727 个 model state key、364 个参数别名、SGD optimizer、scheduler、Python/NumPy/Torch/CUDA RNG、epoch 7/offset 9296/update 22475 数据游标、selected epoch 0 和 71 个 Node ID；该断点位于 optimizer update boundary，没有待累积梯度。模型、optimizer、scheduler 的严格 CPU 加载通过。源保存了一条 CUDA RNG 流，CPU 完整 runtime restore 正确拒绝 1→0 CUDA 拓扑；A100 单卡完整 loader、下一次更新、完整 train/development 回放及 owner 交接仍未执行，`dispatch_allowed=false`。见[脱敏记录](../acceptance/v5_full_resume_static_20260923.json)。

### OCT3D 单写入缓存扩展候选，2026-09-23

只读核验确认现有 generation5 macular operation 是明确的 2-D cache：engine 的 `TRACKS` 仅含 CFP 与 OCT middle B-scan，overlay 标明 `Macular 2-D cache`，逐参与者 receipt 记录 `oct_volume_materialized=false`。它的70,885行身份与 R&B macular train 58,380＋development 12,505 对齐，但完整执行也不会生成 DenseNet3D 所需 `oct_volume_3d.npy`。

当前候选以独立 operation、独立输出和独立非阻塞锁生成 OCT3D；原 2-D operation、状态及输出不可变。输入固定到同一70,885行 raw-row bundle、两份 train/development manifest、预处理 recipe 和逐参与者 OCT3D SHA。原 generation5 writer 必须先接受完整 proposal digest，并发布哈希固定的 adoption receipt；普通候选合同故意使用无效零 SHA，因此不能自行运行或形成第二 writer。状态游标要求精确整数范围及 `completed==next_index`，防止损坏状态跳过参与者。

一眼和双眼真实只读配方回放分别得到 `[1,1,128,224,224]`、`[2,1,128,224,224]` 的 uint8 数组；重建 `.npy` 字节与冻结 manifest 及旧 derived cache 三方 SHA 完全相同。CPU 257 项通过。该结果证明两例数值配方和工程门禁，不代表完整 cache、owner 采纳、资源 profile、GPU 恢复或科学验收；见[脱敏候选回执](../acceptance/oct3d_cache_extension_candidate_20260923.json)。
