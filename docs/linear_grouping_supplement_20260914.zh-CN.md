# 2026-09-14 研究补充与数据准备记录

状态：研究设计登记与只读盘点；不是新训练已派发、数据已迁移或文件已删除的证明。

## Radon_Bridge：信息维数与通信连接

保持纯线性、零初始化残差、原网络及数据协议。保留SVD稠密参照；新增分组卷积作为独立机制因素，不以较高开发分数替换既定研究臂。

1. 固定SVD r32/M32/S64/k3，比较G=1/2/4/8/16。每组必须包含各来源，按每来源的通道分块、保留组内全部M方向；先按(group,source,channel,direction)排列，返回逆排列。不得把按来源拼接后直接groups=2当作跨来源桥。
2. 所有G匹配普通线性重采样，保存实际参数、有效连接、投影张量峰值与吞吐。标准SVD的G1复用仅在完整配置与父模型一致时允许。
3. 无通道压缩+分组卷积与SVD+稠密卷积另作等参数/近似等计算比较。G必须同时满足各来源分块条件。若无合格匹配，报告实际差异，不改变r/M/S或填充通道冒充匹配。
4. 同一基的分组结果可能依赖通道排序；输入/输出同时作固定置换并逆恢复的验证需要区分纯重命名与真正改变分组成员。SVD顺序按能量排序不是跨来源语义对应的证明。
5. 首轮不添加非线性、门控、额外卷积层或自动通道洗牌。新增这些应另立比较。

覆盖原批准九个同疾病同架构双来源组、三个种子。第一项相对G1的新增位置上界为9×3×4×2=216，仍需来源、资源和配置去重；它不是已新增训练数。无压缩匹配组需完成可行性计数后登记。

验收：旧G1严格输出/梯度/恢复兼容；组内跨来源梯度存在；跨组桥直接梯度为零；不得将其外推为全网络完全独立；零初始化保持预测；完整优化器更新、固定基与Node ID；普通通信匹配；资源预检及正式诊断。test保持封存。

## 基础模型留存

只有同一应用定义下的开发集配方比较可产生可部署优选项。分组键至少包括：队列与划分、疾病/任务、输入模态与维度、预处理、模型架构与框架版本、训练制度以及研究要求保留的初始化分层。随机种子是重复维度，不能仅留下分数最高的种子。

永久保留所有尝试的配置、代码/环境、数据来源摘要、训练曲线、指标、停止原因、选优依据与删除清单。受限预测留在授权环境；GitHub不是受限数据与大权重备份的替代品。

保护：运行/暂停/失败/待恢复模型，既定项目引用的父模型，已登记待test及机制诊断的检查点，科学比较需要的不同种子/初始化/资源层级。当前排名不决定其删除。

当前冻结验收要求best.pt和last.pt均存在且SHA匹配。必须先实现和验收可被消费者识别的finalized artifact与位置映射，才能清理停止状态；不能修改旧receipt使删除看起来仍是原验收。落选最佳权重的清理更晚，须等待选型锁定、所需评价完成与全部引用核对。

## 数据接入

两盘已挂载：My Passport UUID 69B9-6FD2；My Book UUID 7E0E-D004。只读扫描确认标准UKB目录含眼科、心脏/腹部MRI、ECG、DXA及颈动脉超声相关字段。尚未确认UKB脑MRI批量来源。Target Project中MRI文件不因位于UKB目录下就被认定为UKB；待来源确认，不复制为脑UKB。

用户当前优先脑数据；尚未擅自改为心脏或全器官迁移。确认来源后保存原始档案、标签与字典、访视/参与者关联和逐文件大小/SHA，增量复制到Ibex既有UKBiobank存储。保留移动盘原件。

本次只读模型留存清单位于Ibex operations/2026_09_10_11_11_31/retention_inventory_20260914/inventory.json；未执行删除。其他器官基础模型可在数据验收后准备，项目方法实验仍优先完成眼科。
