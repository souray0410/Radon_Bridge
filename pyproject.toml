[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "radon_bridge"
version = "2026.9.9.dev0"
description = "Radon_Bridge research application using a fixed MHD API"
requires-python = ">=3.11,<3.14"
dependencies = ["mhd-framework==4", "torch>=2.8", "torchvision>=0.23", "numpy", "pandas", "Pillow", "scikit-learn", "scipy", "matplotlib>=3.10,<3.11"]

[tool.setuptools.packages.find]
where = ["src"]
include = ["radon_bridge*"]

[tool.pytest.ini_options]
testpaths = ["tests/unit"]
addopts = "-ra -p no:cacheprovider"
