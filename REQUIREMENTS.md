# R&B 项目当前约束

更新依据：用户 2026-09-04 的明确纠正及原对话审计。
修改研究架构前读取 experiments/specification_audit/AUDIT.zh-CN.md，区分
完整原文、用户后续覆盖要求、实际实现和待完成事项。

- 原生网络各自保留完整任务路径、输出头、损失与评价；R&B 只通信中间特征。
- 特征节点可来自不同层、维度、通道与网络；完整通信组须可组合、多入多出，
  原参加者原位返回。不能把固定2D/3D pilot 当成通用接口已完成。
- Householder；角度 [0,π)；完整有符号距离支持；投影后 reshape 和 Handoff。
  使用 M/S/H 参考配置与对应 upsilon 控制。默认线性 mixer、无额外门控，
  伴随回投影与逆变换区分，直接残差回各自原生特征。
- 网络阶段和桥接计算显式表达为 MHD，前后向均使用 MHD。
- 分类主要指标为各任务/分支的 macro-F1，辅以 macro precision/recall、
  混淆矩阵等；AUROC只是补充。类别平均与分支平均严格区分。
- 无桥接的各自独立模型是第一对照；必须逐分支报告桥接前后变化，不能用
  单个共享输出或最后概率平均来替代。现有历史数据不能重命名为修正版结果。
- GitHub main 始终最新；更新前旧版归档纯时间戳分支，不用 exp 分支名。
  ws 仅一份当前代码，历史结果外置；运行锁保护部署；每GPU本任务显存<10GiB。
- 两份完整 V1.0 TXT 已保存于 specification/，原文 Givens 由用户的后续
  Householder 指令覆盖；原始文件保持不变。具体覆盖与来源哈希见 specification/README.md。
- 修正版 005 的代码与验收用例已编写；是否通过验收和训练结果以 005 的报告为准。

- 无通信对照应验证训练更新独立性；新训练按任务分别裁剪梯度，并以任务CE之和保持单任务梯度尺度。解冻阶段显式配置，基线稳定前不扩大效果结论。

- 用户进一步纠正：当前主实验必须全网络联合训练，2D/3D主干从stem至stage4、各自头及桥接可学习参数全部更新。桥接位置与解冻范围是独立配置。无桥接对照同样全解冻；部分冻结仅历史诊断，不能冒充主方案。当前全解冻协议BatchNorm仿射参数和运行统计量都更新；固定几何投影算子仍是buffer。

- 用户质疑1/32通信压缩过强：下一批全联合训练以upsilon_H=1/8为主配置，同时比较1/16、1/32；在尚无带宽证据前不能固定最窄瓶颈作为主方法。该对照固定M、S、桥接位置和训练设置。

- 用户明确要求MHD level原位通信：保留既有网络Node ID及原边连接，参与节点经一条R&B超边到一个通信结果节点，再由返回超边在后续level写回原节点；不得额外创建替代native feature节点并改接后续边。通用接口支持K参与者、异构通道与n维特征，自动从示例特征推导形状并调度因果切面；样本对应关系由调用方明确。

- 最新用户要求：每张GPU本任务总显存最多10GiB，两卡可用；先完整审阅新实验/模块设计再训练。当前自动训练暂停，旧35次16epoch协议标为待审阅，不得直接启动。
