## 2026-09-09 最新覆盖：统一命名与Ibex新时间戳

自2026_09_09_10_30_34起，仓库/项目目录使用Radon_Bridge，Python包使用radon_bridge，展示使用Radon Bridge（R&B）。按experiments/2026_09_09_10_30_34准备Ibex；保留旧包、旧绝对路径和检查点兼容，不改历史SHA。Ibex采用souray/home/mengh与souray/data/mengh分离布局，不加projects层。全量影像迁移不中断。用户已暂停两张A100/48小时的申请，新科学协议尚未锁定，本轮不得自动启动训练或重新使用旧test选型。ws02设备0约束只适用于ws02，不覆盖Slurm分配的设备集合。

## 2026-09-08 用户追加：扩展前核查与单卡选择

## 2026-09-08 当前交付报告修订

完整development/validation与test逐项对应；每图给出坐标、单位、误差线、比较方向、解读与限制。主报告+804项附表由验收数据生成，不新增训练、选优或test推理。新报告验收后替换并删除旧47页汇总PDF，更新本地/GitHub/ws02入口，底层实验和预测证据不删除。

后续R&B只派发到物理GPU 0，GPU 1留给他人；运行时配置文件优先，每卡本项目合计仍≤10 GiB。已完成队列不重新启动。

用户授权对开发→test落差与MMTM回到epoch0进行只读核查。检查现有清单、缓存、预测、训练曲线和停止点；固定训练128人调制探针、4配置的训练16人梯度探针，不运行新的test前向、不更新优化器、不新增训练、阈值或模型选择。结论为test后描述性审计，不改变已锁定的主要比较。见 reports/2026_09_08_expansion_audit/README.zh-CN.md；未来数据/模型/训练协议单列设计。

## 2026-09-08 用户追加：Radon本体与傅里叶的通用诊断

在正弦图谱之后增加CPU只读验证：插值场傅里叶切片数值误差及低阶矩、21个选中桥卷积的矩阵频率增益、普通BA空间响应。不新增训练、不改test主要比较、不引入FFT/FBP替代桥。固定探针仍为已有三种子r16的各集合前16人/双眼/前3通道，不称全队列估计。定义、频率单位、边界裁剪、零范数与不可行几何均显式报告。详见 experiments/geometry_mechanism/FOURIER_DIAGNOSTICS.zh-CN.md。

## 2026-09-08 用户追加：只读 Radon 正弦图谱

当前统一test与统计完成后追加21个已有检查点的train/development/test可视化，零新增训练。模型按结构、样本按稳定缓存排序预定；初始与选中状态、投影/自身/跨来源/返回分量分别核验。保持原主假设和科学配置。二维角度与三维球面方向区分，普通重采样不称Radon角度，反投影不称精确逆变换。全队列聚合可同步，个体特征、预测与案例只留ws02受限目录。详见 experiments/geometry_mechanism/SINOGRAM_ATLAS.zh-CN.md。

# 当前test审计范围覆盖（2026-09-07）

用户明确以本批正式实验的统一train1264/development296/test290及模型训练来源为test验收范围，更早独立LOOK实验不自动触发排除或重划分。早期使用记录保留，不宣称全研究历史未使用；本批预训练/训练/选优链仍须排除test，模型与比较锁定、严格加载和开发重放仍必须通过。见UNIFIED_TEST.zh-CN.md末节。

# 当前运行覆盖：任意指定GPU集合（2026-09-07）

按 experiments/geometry_mechanism/GPU_ALLOCATION.zh-CN.md 支持任意实际GPU编号组成的单卡、双卡或多卡运行时选择；当前0/1。减卡正常排空当前任务，加卡继续派发，空集合暂停新派发。每张物理卡上本项目工作进程合计≤10GiB，LOOK等其他项目不计入项目上限但仍检查设备可用空间。监控不得覆盖用户选择，科学配置及已接受结果不变。

# 当前执行覆盖：72项补充后追加96项多深度机制验证（2026-09-07）

最新用户授权在审核后排队执行 experiments/geometry_mechanism/MULTIDEPTH.zh-CN.md：stage2/3/4全部7种非空组合、两种通信、3种子、两学习率，外加两档近似参数匹配单桥；96项新训练、18个复用参照。当前72项按原不可变版本继续，完成并释放项目锁后新队列拟合训练集stage2/4基、执行batch16资源及诊断验收，通过才正式训练。新增多桥每桥分别裁剪5，不套用单桥梯度零余弦。22主要/38次要比较事先锁定。原末端可学习融合仍撤销；test需等待两队列、统一注册/访问审计/开发重放及完整3D数据验收，不得因旧72项完成而提前解封。三小时监控与10GiB每卡、LOOK/数据/归档边界不变，不自动继续扩展矩阵。

# 当前执行覆盖：用户授权空闲GPU即时续派

按 experiments/geometry_mechanism/ROLLING_DISPATCH.zh-CN.md 改进调度：训练进程仍各卡独立，取消两任务整批等待，逐项后台验收归档。实验矩阵、训练参数、显存与收敛标准不变；安全排空当前任务后在隔离新版本切换。

# 当前执行覆盖：机制子集已授权开跑

用户明确要求接上开跑。执行 experiments/geometry_mechanism/EXECUTION.zh-CN.md：七个预设几何点、两预算/两协议/两LR/三种子，历史接受后去重；固定MMTM256/注意力256宿主与h512参考桥，无最佳配置选择。该修订覆盖下文“矩阵待锁定/不得启动”状态；全部训练/数据/资源/归档标准保留。

# 最新覆盖：2026_09_06_14_05_08 机制基准与导师汇报衔接

用户先批准几何／资源／双评价协议／宿主增强方案，随后明确修正：后续将更换网络和数据，本轮无需寻找当前队列的最佳配置，而应验证可解决的关键机制问题。研究问题与证据边界见 experiments/geometry_mechanism/RESEARCH_QUESTIONS.zh-CN.md。

本轮先完成完整历史归档及严格恢复验收、可变 kernel_size（默认3）与显式 r/h、旧路径兼容、同输入并行残差和资源计数。尚未锁定修订后的训练矩阵；不得自动启动此前数千项全排列或按开发集最优筛选增强配置。原两评价协议、父模型、batch16、平台标准、10 GiB／GPU、双卡独立派发、LOOK和test边界保留。历史文件原样保存至已授权备份盘，验收前不删除原副本。

# 最新覆盖：18项S轴邻接与低成本原理验证（2026-09-06）

用户授权执行 experiments/s_axis_supplement/PROTOCOL.zh-CN.md。必须等待54项任务融合及报告全部完成再运行新增18项；沿用旧双分支机制选优/损失。独立CPU验证可提前，GPU验收/部署等待锁。原约束、历史证据及默认检查点兼容保持；完成后冻结方法候选与主张，不继续自动扫描。

# 最新覆盖：任务融合公平基准（2026-09-06）

用户授权适配当前CFP/OCT场景补充公平对照。执行 experiments/task_fusion_benchmark/PROTOCOL.zh-CN.md：54项新训练，统一融合CE及两路原生辅助CE、融合F1选共同检查点；保留历史独立预测器机制研究，不混合指标。复用三种子父检查点和SVD基，9配置×两档主干LR×三种子，原数据、batch16、平台、10GiB、双GPU及LOOK边界不变。该明确新授权覆盖旧“暂不增加新实验臂”，原路径默认行为保持兼容。

# 当前覆盖：57项机制与常用方法补充

用户批准执行 experiments/mechanism_benchmark/PROTOCOL.zh-CN.md。保留129项，新增57项，最终186项；只用3416–3418。新增单向cross_edges、固定QR重采样、MMTM和双向交叉注意力适配；默认Radon及历史检查点兼容。保持原预训练父检查点、全参数BN更新、batch16、10GiB与平台规则。历史“不增加单向”等限制由本轮明确授权覆盖。暂不增加新实验臂；本轮是机制基准，未来独立验证另立协议。

# 当前生效要求：2026_09_04_20_27_40

## 2026_09_05 用户追加：可学习原生通道压缩与恢复卷积

- 新增learned_channel，逐来源无偏置1×1（nD共享逐点）编码C→r和独立恢复r→C；编码在Radon前、恢复在普通反投影后，保留方向/S轴、线性kernel3通信与原Node ID残差。无激活、BN、门控或偏置。
- 编解码初始Q^T/Q使用与已接受随机QR完全一致的seed/来源/C/version规则，不读训练特征、不消耗全局RNG；两端是独立参数，训练后不绑权、不限制正交。中间mixer零初始化。固定路径仍仅mixer可学习；用户在本新增可学习臂授权两端也训练。
- 三个种子3416/3417/3418 × rho1/16、1/8、1/4 × 标准Radon、自身、空间打乱、普通线性重采样，共新增36项；r16/32/64、h512/1024/2048。与已有非中心化/中心化各36项对应，并保留无桥/CM/QR参照，总129项。没有新增独立预训练、SVD拟合、随机基扫描或冻结实验。
- 沿用stage3/M32/S64、backbone6e-5、head/bridge1e-4、batch16、AdamW WD.01、分支及桥裁剪5、全参数训练及BN更新、至少8轮/6轮无>.001改善/3轮LR×.3/60轮保护上限。GPU时长不限，每卡本项目≤10GiB，不动LOOK、不读测试。
- 隔离检出验证并同步GitHub；等待中心化93项队列及报告完成、项目锁释放后，接受93项配置/父检查点/平台/预测/摘要SHA。四模式rho1/4完整GPU前后向优化及1264人能量/128人梯度预检通过才部署训练，不修改运行源码或覆盖历史。
- 新增36项训练与36项只读诊断分别记账。诊断以当前编码矩阵行空间的正交投影统计原始能量和中心化方差保留率；实际编码能量增益另列，不将大于1的幅值放大叫作信息保留。记录编码谱/数值秩/编解码SHA及恢复与编码转置差异。
- 全部129项统一报告；新可学习通道减同机制非中心化/中心化，标准Radon另减无桥/原CM/随机QR/新自身打乱重采样。10000次参与者配对bootstrap跨种子共享索引，3种子均值与样本SD、逐分支/逐种子、3418单列。配对QR同时放开正交、转置绑定和学习；对比SVD还改变初始化，不称为只改变是否学习。仍是开发集探索证据。


## 2026_09_05 最新追加：中心化拟合基配对补充

- 用户明确追加中心化与非中心化对应实验。沿用全部四类SVD桥（标准Radon、自身处理、空间打乱、线性重采样）×三个种子3416/3417/3418×ρ1/16、1/8、1/4，新增36项第二阶段训练，与现有36项非中心化SVD配对；加其余21项已有基线/可学习/QR，最终93项不同结果。
- 本轮唯一新变量是拟合基统计量：中心化使用E[xxᵀ]−μμᵀ，原版使用E[xxᵀ]。μ按训练集所有参与者、眼和空间位置逐通道统计，不是逐患者减均值。运行时两者均QᵀX，返回QΔ，不减/加均值；只比较中心化拟合，不声称检验完整中心化PCA编解码。
- 新增compression=fixed_centered_svd_channel；原learned_projected、fixed_svd_channel、fixed_random_orthogonal_channel保持兼容。保存完整基、均值、原始二阶矩、中心化协方差、方差谱和原始能量、来源及SHA。相同来源/种子各ρ共享完整基的嵌套前r列。ρ是维数比例，r16/32/64，每路h512/1024/2048。
- 不增加独立预训练网络。复用当前三个种子的独立最佳父检查点，各重新统计一次训练集得到中心化基（3项拟合）；核验训练参与者顺序、原始二阶矩、原生状态和父检查点SHA与原基一致。
- 主干6e-5、head/bridge1e-4、stage3/M32/S64、kernel3无偏置零初始化、batch16、AdamW/WD0.01/分支桥分别裁剪5、全参数与BN训练及原平台规则全部不变。每卡项目≤10GiB，GPU时长不限，LOOK和测试边界不变。
- 当前57项队列继续，不修改运行源码。隔离检出验证同步GitHub，排队等其报告完成且释放项目锁。拟合、最大ρ四模式前后向优化更新及完整128人/1264人诊断预检通过后才部署执行；失败/超上限/未平台需处理，不自动改变训练标准。
- 中心化36项与非中心化36项都用新诊断代码重新统计，共72项完整诊断。分开报告原始能量保留率、中心化方差保留率、初始与训练后统计，另保留残差及桥/主干梯度。只读诊断不改参数、BN、优化器或随机状态。
- 配对方向统一为中心化拟合减对应非中心化拟合；10000次参与者bootstrap跨种子共享索引；报告三种子均值/样本SD、逐种子值、3418单列、原两个种子参与筛选、CFP/OCT/平均、负结果、成本/轮次/显存/基与父检查点SHA。最终导师报告整合93项并做视觉审核。仍为开发集探索证据。

## 2026_09_05 最新覆盖：三个种子，所有机制补齐三档ρ

- 总共只用3416、3417、3418；取消尚未启动的3419/3420。六种桥（可学习CM、SVD Radon、SVD自身、SVD空间打乱、SVD线性重采样、随机QR Radon）均为ρ=1/16、1/8、1/4；同种子无桥共享，共57项第二阶段结果。
- 复用原14项匹配历史结果和已完成4项机制训练；本队列新增39项第二阶段及3418一次双分支独立预训练，共40项训练。全部57项补充完整只读诊断。
- 第二阶段主干6e-5、head/bridge1e-4、stage3/M32/S64、batch16、10GiB上限与原平台规则不变。基按同种子同来源复用；不同ρ使用嵌套列。
- 原五种子队列在只读诊断因计算图未及时释放而OOM，4项机制训练已完成。保留失败与成本，只在新目录重做诊断。未触碰训练算法；诊断释放MHD前向缓存，在两个CE梯度累计后执行，不更改参数/BN/随机状态；用全部训练能量和128人探针验证。
- 报告统一三种子均值和样本SD，3418单列（单种子SD未定义）；同ρ SVD减六对照，10000次参与者配对bootstrap跨种子共享索引。57项不重复计无桥。全部负结果、CFP/OCT/平均、成本、轮次和SHA完整交付。开发集探索证据；原两个种子参与配置筛选。
- GitHub同步并用时间戳分支归档；获得项目锁并通过最大ρ完整GPU预检后部署。PDF/图表在最终数据齐全后做视觉审核，自动生成完整表格与报告并同步本地。

## 2026_09_05_09_03_55：五种子机制验证（用户已批准实施）

- 固定3416–3420五种子，第二阶段主干6e-5、head/bridge1e-4、stage3/M32/S64。无桥、learned rho1/8、learned rho1/4、SVD Radon、自身、空间打乱、等宽线性重采样、随机QR通道基Radon八臂。固定版本rho1/8、r32、h1024。复用旧种子8项结果，新增32项第二阶段及3项独立预训练。
- 新种子独立预训练主干3e-5、head1e-4，原平台规则。新增模式只改变桥机制；桥内仅kernel3无偏置零初始化卷积可学习，BN及原生网络全参数训练。
- SVD各控制共用每个来源的训练集固定基；QR使用CPU float64局部生成器，由seed/来源/C/version的SHA256独立确定，QR对角符号规范，不按能量排序。几何固定；Q为持久化buffer。
- 等宽重采样展平空间后线性插值L↔32×64（align_corners=False），前向与返回分别匹配标准Radon/普通反投影行L2范数；32仅打包维度，不是角度。自身处理使用块对角掩码，报告存储与有效参数。
- 独立只读诊断初始和选中检查点：全部训练参与者的写回前通道能量及残差；固定排序前128训练参与者累积梯度后计算stage3与桥子模块的范数/余弦，零范数为未定义。固定单桥卷积梯度行块正交作为结构核验，不启用PCGrad。
- 不增加单向通信、损失、增强或M/S/LR/rho扫描。保持batch16、AdamW WD0.01、分支/桥裁剪5、至少8轮/6轮无>0.001改善/3轮LR×0.3/60轮保护上限。GPU时间不限但记账，每卡项目≤10GiB，不影响LOOK，不读测试，失败或未平台需处理。
- 全部方法固定配对报告SVD主方法减七参照；五种子均值和样本SD、新增三种子单列。10000次参与者重采样跨种子共享索引，不能将重复种子视作独立参与者。记录数据、父检查点、基、预测、摘要SHA，自动报告和本地收取。


## 2026_09_05_01_29_50 用户最新确认：训练集 SVD 固定通道压缩对照

- 追加12项标准Radon桥：backbone LR 3e-5/6e-5 × rho 1/16、1/8、1/4 × seed 3416/3417；stage3、M32、S64、head/bridge LR 1e-4。复用前轮12项可学习R&B及4项无桥结果。不追加固定版机制对照，不使用rho1/2。
- 后续讨论将原先的随机高斯QR基方案改为训练集SVD基；不再生成随机QR基。SVD同样是正交结构，按能量排序。rho控制维数r=floor(rho*C)，不以90%累计能量自适应改变宽度；报告各档实际能量保留率。
- 每个种子从其两路独立最佳检查点，在eval/no_grad模式仅用1264训练参与者拟合native stage3非中心化通道二阶矩FF^T/N，CPU float64特征分解按能量降序。C256完整基各拟合一次，两学习率共用、不同rho取嵌套前r列；拟合时BN不更新，随后联合训练BN正常更新。
- Q作为持久化buffer：Q^T X → Radon → 可学习kernel3无偏置零初始化Conv1d → 普通直接反投影 → Q解码 → 残差写回原Node ID；桥内只有中间卷积可学习，两条原生网络全部训练。默认learned_projected路径兼容，新增fixed_svd_channel和带SHA256的basis_files；固定版首轮仅支持标准Radon。
- 完整32项及项目锁释放后，隔离检出拟合基与rho1/4 batch16 GPU预检；通过后部署、追加12项。每卡本项目≤10GiB，不打断其他LOOK任务，GPU时长不限但继续记账。不改变batch、优化器、梯度裁剪或平台规则。
- 达60轮未平台、OOM或失败一律需处理，不能当作完整结果。报告10000次参与者级配对bootstrap，CFP/OCT/平均绝对F1与两种差值，参数/耗时/显存/轮次/基与父检查点SHA。拟合成本单列。
- 能量保留不保证判别信息保留；联合训练可能改变特征分布，固定基不再保证每轮能量最优。本对照同时改变投影结构与两端可学习性。两种子包含训练及对应预训练特征/SVD基差异，不再含额外随机QR基随机性。仅开发集探索证据。


## 2026_09_05 用户补充：机制对照也纳入本轮

- 在16项backbone学习率/ρ矩阵后追加random、scrambled、self、pooled四种机制对照，两个backbone学习率、两个种子，共新增16项、总计32项。
- Radon类对照stage3/M32/S64/ρ1_8，复用本轮对应seed/LR的无桥与标准R&B；不混用历史M16。
- 池化的M/S不适用，实际h不同，报告中明确非严格容量匹配。源码待当前队列结束再部署。

## 2026_09_05 用户最终确认：暂不运行ρ=1/2

- ρ=1/2的batch16预检触及10 GiB显存限制，用户明确先不用此档。
- 最终矩阵为两个backbone学习率3e-5/6e-5 × 两个种子3416/3417 ×（无桥+ρ=1/16、1/8、1/4），共16项新实验。
- 时长不限；其余固定条件与10 GiB显存上限不变。

## 2026_09_04_23_54_10 用户追加：两个backbone学习率（当前最终矩阵）

- backbone比较原值3e-5和提高一倍的6e-5；任务头、桥均1e-4。
- 两个backbone学习率×两个种子3416/3417×（无桥+四档ρ），共20项。其余stage3、M32、S64、batch16及收敛标准保持一致。
- 低学习率3e-6仅保留历史负结果，不再作为本轮实验臂。

## 2026_09_04_23_54_10 用户追加：取消时长预算（覆盖所有历史分钟上限）

- GPU为用户自有，明确取消GPU时长预算和240 GPU分钟累计上限，以完整实验和预设收敛标准为完成条件。继续记录耗时，但不再以分钟预算阻止或截断实验。
- 两个种子3416/3417各自重新运行无桥及ρ=1/16、1/8、1/4、1/2完整五臂，共10项。恢复原学习率，固定M32/S64/stage3，其他条件不变。
- 每卡10 GiB显存与不干扰其他任务约束保留；最大60轮为原有收敛保护上限，不是时长预算。未收敛不得视为完整结果。

## 2026_09_04_23_54_10 用户最新调整（当前生效）

- 恢复主干3e-5、任务头1e-4、桥1e-4。低原生学习率方案退出主实验，保留负结果及费用溯源。
- 固定stage3、M=32、S=64，只扫描ρ=1/16、1/8、1/4、1/2；两来源用相同标量。其他训练设置不变。
- 已完成且严格匹配的无桥与小ρ结果可复用，明确来源；新增大ρ对照并自动汇总完整规范图表，名称统一R&B（Radon Bridge）。
- 沿用240 GPU分钟累计上限、每卡10 GiB、batch16、完整平台期规则。先核验大ρ显存，不以资源受限冒充性能结论。不恢复周期提醒。

## 2026_09_04_22_32_30 用户补充

- 自动续跑保留历史账目，使用总计240 GPU分钟内的统一剩余额度，不再以旧分组额度阻止已授权的新一轮。
- 新一轮主干3e-6、任务头1e-5、桥1e-4，从原独立训练最佳检查点做无桥/M32匹配对照，其他训练设置不变。
- M和rho应支持按来源分别配置；日常快速实验默认统一标量。本轮两路仍为M32/rho1_8，不启用异构角度实验。
- 不修改正在运行的源码；待原队列释放锁后部署新版本。不恢复已取消的周期提醒。

用户最新授权优先于 specification/ 内原始说明书和历史审计。原说明书保留原样，不用其旧 Givens、比例角网格或伴随返回覆盖当前 Householder EEM 与普通直接反投影。

- 任意K个异构中间特征，M/S正整数，rho为(0,1]压缩比例。保留宽度max(1,floor(rho*C_i*M))，不同桥可独立配置；首轮16/64/0.125，stage3单桥。
- MHD原生阶段、前后向、原Node ID及原下游连接；通信结果包经返回超边在后续level写回。每条完整网络有自己的头、损失和预测。
- 独立模态有监督训练达到预先声明的验证平台期后，再从各自最佳检查点做全网络桥接训练。所有可学习参数训练、BN更新，不冻结主干。固定几何为buffer。
- 第二阶段共同检查点、样本顺序、优化初值和收敛规则，分别比较无桥、R&B、固定高斯随机投影、空间打乱、自身处理、普通池化通信。随机控制为自定义机制对照，非已发表Random Bridge复现。
- 各分支macro-F1主要；报告precision/recall、混淆矩阵、参数量、时间、显存、选中轮次和停止轮次。只把达到平台期的试验算完整，不将epoch上限或预算耗尽称为收敛。
- ws02每卡本项目合计<=10GiB，不干扰其他任务；继承账目，当前剩余预算见protocol.json，历史累计上限240 GPU分钟。本轮原60分钟限制不因修订重置。
- main最新、历史纯时间戳分支、远端仅当前代码。删除无效历史性能输出，保存成本与必要缺陷溯源；不删除原始数据、有效缓存和初始化权重，不重写Git历史。
- 使用现有1264/296配对缓存，不读取测试；开发集已历史筛选，只能形成探索性证据。原始UKB未挂载时不声称已生成新高分辨率数据。
- 用户已授权本次修改、同步、部署、实验及每30分钟监控。监控只在有实质变化或需要处理时通知。
