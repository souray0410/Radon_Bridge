# 版本与数据管理

GitHub main维持最新代码、协议、验收与去标识化汇总。每次研究协议修订创建Riyadh纯时间戳目录与分支；更新前旧main已有对应纯时间戳快照。ws02仅保留/home/mengh/RadonBridge当前代码，部署前检查运行锁及干净工作区，部署后核对commit/协议SHA/源码SHA。数据、检查点、参与者预测只在/data/mengh/RadonBridge。

无效旧输出从main工作树及远端活动运行目录移除；远端残留旧诊断/验收已可恢复归档，永久删除被自动审批拦截；Git时间戳历史不重写。成本与缺陷证据保留，删除结果不归零预算。临时验收源码用后删除。每30分钟监控，有完成/失败/需要处理才通知，运行器实时执行显存与时间边界。
